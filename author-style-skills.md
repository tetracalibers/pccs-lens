# 文体スキルの仕組み（author-style-analyzer / author-style-writer）

著者の執筆スタイルを「分析して再利用可能なガイドに落とし込む」スキルと、「そのガイドを使って著者らしい文章を書く」スキルの2本立てで構成しています。このドキュメントは、両スキルの仕組みと内部の役割分担をまとめたものです。

- **author-style-analyzer** … 過去記事とGit履歴を分析し、著者固有のスタイルを4つのガイドへ抽出・更新する（生成側）
- **author-style-writer** … 生成済みのガイドを参照し、著者らしい文章を生成・推敲・レビューする（利用側）

## 全体像（データフロー）

```
        過去記事（+page.svx など） + Git のコミット履歴
                        │
                        ▼
            ┌───────────────────────────┐
            │   author-style-analyzer   │  Workflow で観点ごとに独立分析し
            │        （分析側）         │  反証レビューで検証（決定論実行）
            │  根拠モード / 発見モード  │  ＋ scripts/ の集計・記帳スクリプト
            └───────────────────────────┘
                        │ 生成 / 差分更新
                        ▼
                writing-guides/
                  ├─ thinking-flow.md      … 思考の流れ      （TF-001…）
                  ├─ writing-style.md      … 文章構成        （WS-001…）
                  ├─ stylistic-quirks.md   … 表現上の癖      （SQ-001…）
                  ├─ refine-style.md       … 修正傾向        （RS-001…）
                  ├─ evidence/<観点>/<slug>.md
                  │                        … 記事1本1ファイルの根拠（analyzer が読み書き）
                  ├─ pending/              … 各ガイドの保留プール（analyzer が読み書き）
                  └─ STYLE-ANALYSIS-TASKLIST.md
                                           … 記事ごとの分析済みチェックリスト（analyzer が読み書き）
                        │ 参照（syntax-guide・math-notation-guide +
                        │        モードに応じた本体ガイドのみ。
                        │        evidence/ と pending/ は読まない）
                        ▼
            ┌───────────────────────────┐
            │    author-style-writer    │  + writing-guides/syntax-guide.md
            │        （利用側）         │  + writing-guides/math-notation-guide.md
            └───────────────────────────┘    （どちらも記法・書式の必須ルール）
                        │
                        ▼
            著者らしい記事本文（生成 / 推敲 / レビュー）
```

分析側が「知識（ガイド）」を作り、利用側がそれを「消費」する一方向の関係です。両者は `writing-guides/` を介して疎結合になっています。

## 成果物：4つのスタイルガイド

analyzer が生成し、writer が参照する中心的な成果物です。いずれもリポジトリ直下の `writing-guides/` 配下に置きます。責務を混同せず、同じ特徴を複数ファイルへ重複記載しないことが原則です。

| ファイル | 扱う対象 | 問い |
| --- | --- | --- |
| `thinking-flow.md` | 完成文からリバースエンジニアリングした著者の判断と思考の流れ | なぜそう書くか |
| `writing-style.md` | 記事・セクション・段落・文の構成上の傾向 | 文章上、どこに何が置かれるか |
| `stylistic-quirks.md` | 頻出する言い回し・語尾・接続表現・表記などの癖 | どのような言葉で書かれるか |
| `refine-style.md` | AI草稿を人間が修正した際の追加・削除・並べ替え・書き換えの傾向 | AI草稿をどう変更するか |

一つの現象が複数の階層に現れる場合は、抽象度に合わせて各ファイルへ分解します（同じ文の複製はしない）。分類に迷う特徴は、各分析結果を比較してから配置先を決めます。

### 執筆側と分析側の2層構造

成果物は、読む主体で2層に分かれます。**この分離が設計の中心です。**

| 層 | ファイル | 読む主体 | 置く内容 | 量の増え方 |
| --- | --- | --- | --- | --- |
| 執筆側 | 4ガイド本体 | writer | 実行可能なルール・適用条件・確度ラベル3語 | **ルール数に比例**（記事数に比例させない） |
| 分析側 | `evidence/`・`pending/` | analyzer のみ | 根拠・確度の判定理由・記事単位の記録 | 記事数に比例してよい |

かつては根拠と確度の判定理由をルール欄に散文で書いていたため、分析ラウンドごとに追記される台帳になり、4ガイドが計 313,714字（242ルール）まで膨らんでコンテキストを圧迫しました。実行可能な指示は全体の16%で、分析メタデータが66%を占める状態でした。根拠を `evidence/` へ、判定理由を確度ラベルへ畳んだ結果、4ガイド計は大幅に縮んでいます（経緯は `spec/writing-guides-compaction.md`、作業記録は `spec/COMPACTION-TASKLIST.md`）。

**この「量の増え方」は、ラウンドごとに測って報告します。** analyzer は分析のたびにガイド本体へ追記し、その本体は writer が毎回全文を読むので、増えた分はそのまま執筆側のコンテキスト負荷になります。放置すると圧縮前と同じ経路で膨らむため、`scripts/style-guide-size.mjs` でファイルごとの文字数の増減%・ルール数の増減・合計・1ルールあたりの平均文字数を出し、**肥大化率を完了報告に必ず載せます**（測定対象は writer が読む6ファイル。`syntax-guide.md` と `math-notation-guide.md` は analyzer が触らないので `±0%` の参考値として合計に含めます）。**ルール数が増えていないのに本文が5%以上増えたファイルは警告になります**。ルールが増えて本文が増えるのは設計どおりですが、ルールが増えないまま太るのは根拠・確度の説明が本体へ漏れている兆候なので、報告の前に該当箇所を `evidence/`・`pending/` へ移します。1ルールあたりの平均文字数は、ルール数と本文量の比が崩れていないかを1つの数字で見るための指標です。

### ルールID

各ルールの見出しには安定した ID が付いています（`### [WS-012] ルール名`）。観点略号2文字＋連番3桁で、`TF` / `WS` / `SQ` / `RS` の4系列です。

- **ID は根拠から支持記事数を機械集計するためのキー**です。ルール名を改名しても ID は変えず、廃止したら欠番にします（採番の再利用はしません）。
- 採番は `scripts/style-rule-ids.mjs --write` が行います。エージェントも人間も手で番号を決めません。
- 導入の理由は、ルール名がキーとして機能していなかったことです。規約は「ルール名は本体の `###` 見出しと一字一句一致させる」でしたが、実態は `ヘッジ文末` のような短縮形で書かれており、機械照合すると `stylistic-quirks` の76ルール中22件が「支持0件」と出ていました。人間的な照合で破綻が隠れていた状態です。
- writer は ID を**無視して読めばよい**（生成する本文へ ID を書きません）。本体内の `関連ルール` 参照は名前のままで、ID にはしません（writer が読むファイルであり、ID だけでは人間が辿れないため）。

### 根拠（`writing-guides/evidence/<観点>/<slug>.md`）

各ガイドのルールを支持する記事の索引です。**記事1本につき1ファイル**で、ファイルパスがそのまま slug に対応します（`evidence/writing-style/cg/basics/anti-aliasing.md` ＝ `/cg/basics/anti-aliasing`）。

```markdown
分析時点: `d8bd3563`
記事タイプ: 概念解説

- WS-004（末尾を結果要約「なめらかに見えるようになります」で閉じる）
- WS-010（前提記事へリンクし前回の欠落を述べてから問いへ）
  ※ WS-032：スーパーサンプリングは前手法の限界提示を伴わず、限界駆動が弱い
```

- **`- <ルールID>（該当箇所）` の行だけが支持**です。`※` 行は反例・除外の注記で、支持には数えません。
- `分析時点` は、分析した時点の記事コミット（`git log -1 --format=%h -- app/src/routes/<slug>/+page.svx`）の短縮SHA。**現在の最終更新コミットと違えば、その記事は分析後に改稿されている＝再分析の候補**です（タグ改名のような一括修正でも SHA は変わるため、`git diff` で本文差分を確認してから判断します）。同じ記事なら4観点で同じ値になり、食い違いは更新漏れのサインです。
- `記事タイプ` は分析時に分類した種別。移行で作られたファイルは `未記録` のままで、再分析した時点で埋まります。将来2軸での確度導出へ戻す余地を残すための記録です。
- **確度は導出値**です。支持記事数は `scripts/style-evidence-tally.mjs` が数え、ガイド本体には書きません（書くと再び追記対象になります）。
- **記事単位の再分析（撤回）ができる**のがこの構造の利点です。改稿された記事は、4観点ともその記事のファイルを丸ごと書き直します（他の記事のファイルには触らない）。1ファイル1記事なので、新規記事は Write だけ、再分析は該当1ファイルだけの読み書きで済みます。根拠がルール側に散文であった頃は、「シリーズ10本すべてが〜」のような集約表現から1本分だけ減らすことができませんでした。
- 各観点の `_README.md` に、位置づけの注記（analyzer 専用／writer は読まない）と、その観点固有の確度判定基準（一般技法との重なりの扱い、記法・共有コンポーネント由来の抑制など）を置いています。
- **移行で作られた根拠は意図的に不完全**です（集約表現の根拠を個別記事へ展開できていない）。したがって支持記事数が少ないことは根拠が弱いことを意味しません。確度の降格を機械的に当てるときは `--round` で今回書き直した記事に関係するルールへ絞ります。
- 保留プールとは役割が異なります。`pending/` は主要ルールに**採用していない**観察、`evidence/` は**採用済み**ルールの根拠を置く場所です。

### 保留プール（`writing-guides/pending/`）

4ガイド本体には、生成時に適用する主要ルール（強い傾向・条件付きの傾向）だけを置きます。根拠不足・単一記事偏り・一般技法との切り分け困難などで主要ルールに満たない観察（保留）は、本体ではなく各ガイドに対応する保留プール `writing-guides/pending/<同名>.md`（`pending/thinking-flow.md` など4ファイル）に置きます。

| 本体 | 保留プール |
| --- | --- |
| `thinking-flow.md` | `pending/thinking-flow.md` |
| `writing-style.md` | `pending/writing-style.md` |
| `stylistic-quirks.md` | `pending/stylistic-quirks.md` |
| `refine-style.md` | `pending/refine-style.md` |

- 保留プールは**分析専用の補助記録**です。`author-style-writer` は読まず、生成・推敲・レビューの根拠に使いません。
- `author-style-analyzer` は保留プールを、①根拠が増えた保留を本体ルールへ**昇格**、②新たな保留を**追記**、③不要になった保留を**棄却**、の対象とします。本体と保留プールで同じ特徴を重複させません。
- **保留は1項目1行**で書きます。

  ```markdown
  - SQ-P045｜支持2記事のみ・媒体機能への依存が大きい｜支持: /color-theory/color-area-proportion, /color-theory/hue-tone-difference
  ```

  全角縦棒で「保留ID｜保留の理由｜支持記事」の**3欄ちょうど**に分けます（`style-pending-promote.mjs` は3欄で解析するため、4欄目を足すと旧形式として数えられます）。何を保留したのかが読めなくならないよう、特徴名は理由欄の先頭に `<特徴>：<保留の理由>` の形で埋め込みます。**支持記事を機械的に読めることが、昇格判定を機械化できる条件**です。1項目200〜400字の散文で書いていた頃は、昇格の鍵になる支持記事数が文中に溶けていて、判定するには全文を読むしかありませんでした。その結果、規約上は「①昇格②追記③棄却の対象」でありながら実態は追記専用の倉庫になり、4ファイル計523項目・146,537字（採用済みルール251件の2倍）まで溜まりました。1行化の移行では、既存ルールで扱える・根拠の誤読だった・他項目と重複する保留を棚卸しし、266項目・48,032字へ整理しました。
- **昇格の閾値は支持3記事以上**（`scripts/style-pending-promote.mjs` が候補を出します）。件数は必要条件にすぎず、単一シリーズに閉じている・一般技法と切り分けられない候補は昇格させません。昇格の判断そのものは機械化しません。降格の閾値（2記事以下で弱い傾向へ）と1記事ぶん離してあるのは、昇格した直後に降格対象へ戻る往復を避けるためです。
- 各保留プールの先頭には「このファイルの位置づけ」注記（writer は読まない／analyzer が昇格判断で読む）を必ず残します。
- 保留の根拠は保留プールの項目内に書き、`evidence/` へは登録しません（根拠に載るIDは、ガイド本体に存在するルールだけを指すため）。
- 本体4ガイドから `` `pending/<観点>.md` `` への参照は置きません。writer はたどれないため、執筆側には純粋なノイズになります（関連ルール参照は裸の参照のみ）。

### 文体解析タスクリスト（`writing-guides/STYLE-ANALYSIS-TASKLIST.md`）

どの記事を分析済みかを記事単位のチェックボックスで記録した進捗リストです。ガイドではないので `author-style-writer` は読みません。`ogimage/OGP-TASKLIST.md` と同じ位置づけ・同じ凡例で運用します。

- 記事の行を、スキルへ渡せるスコープ（`#<id>`）ごとの見出しでまとめる。色の理論・色の活用分野は YAML のトップレベルカテゴリ単位、CGと画像処理は `sections[].id`（セクション）単位で、いずれもコンテンツ YAML の並び順に従う
- 凡例は `[x]`（分析済み）／`[ ]`（未分析）／`[draft]`（`draft: true` で分析対象外）／`[ページ未作成]`（ページが存在しない下書き）。draft・未作成のものも並び順を保つため一覧に混ぜて載せる（チェックボックスは付けない）
- 掲載対象は解説記事（`layout: guide-content` の `+page.svx`）のみ。トップ・一覧ページ、ゲーム、慣用色名マップ、配色シミュレータなど記事以外のページは分析対象ではないので載せない
- analyzer は**スコープの解決**（未分析はどれか、指定範囲に分析済みが混ざっていないか）と、**分析後のチェック更新**にこれを使う。更新はメインセッションの後処理の責務で、Workflow のスクリプトや統合エージェントは触らない

### 記法・書式の必須ガイド：`syntax-guide.md`・`math-notation-guide.md`

`writing-guides/syntax-guide.md` と `writing-guides/math-notation-guide.md` は上記4ガイドとは性質が異なり、**記事本文の記法・書式（独自ディレクティブ、`:Anki[]`、数式、コード、禁止事項など）を定めた必須ルール**です。著者の文体に関わらず常に従います。writer はこれを最優先級で参照します（後述の優先順位を参照）。

2ファイルの分担は、**数式（KaTeX）の記法と、その表記に関わるインラインコードの規約（数字のインラインコード化・前後の空白）が `math-notation-guide.md`、それ以外の記法（ディレクティブ、`:Anki[]`、見出しの分類タグ、リンク、コードブロック・擬似コード、禁止事項）が `syntax-guide.md`** です。仕上げチェックリストもそれぞれのファイルが持ちます。

強調の記法については、**writer が使うのは `:Anki[]`（暗記モードで隠れる強調）だけ**という線引きがあります。同じ見た目で暗記モードでは隠れない `:Mark[]` も記法として存在しますが、どの語をそれにするかは著者（人間）の判断領域なので、writer は自分から `:Mark[]` を書きません（既存の `:Mark[]` は外さず `:Anki[]` へ変えもしません）。詳細は `syntax-guide.md` ルール1。

#### 機械的に検査できるルール：`npm run lint:svx`

`math-notation-guide.md` のルールのうち4つと**表記揺れの統一**は textlint で検査でき、writer は**どのモードでも本文の変更を終えたら `npm run lint:svx`（`app` ディレクトリ）を通してから報告します**。ルールの実装は `app/textlint/`（`rules/` と共通のマスク処理 `lib/svx.js`）、表記揺れの辞書は `app/textlint/prh.yml`、使い方と設計の詳細は `app/textlint/README.md` にあります。

| ルール | 内容 |
| --- | --- |
| `svx-number-in-code` | 数字は必ずインラインコードにする |
| `svx-math-function-in-code` | 数学で定義されている関数名（`sin`・`cos` など）もインラインコードにする |
| `svx-no-space-around-code` | インラインコードの前後には空白を置かない |
| `svx-inline-math-spacing` | インライン数式（`$$...$$`）の前後には半角スペースを置く |

表記揺れは `prh.yml` の辞書（`textlint-rule-prh`）で統一します。主な項目は `もともと`→`元々`、形式名詞の `ぶん`→`分`、`あいだ`→`間` などで（全項目は `app/textlint/README.md` の表）、項目を増やすときは辞書に追記します。辞書には `specs`（置き換わってほしい例・**ほしくない**例）を書いておき、textlint の起動時に検証させます。writer は**指摘どおりに直し、辞書を書き換えて指摘を消すことはしません**（辞書に載せる表記を決めるのは著者）。

`npm run lint:svx:fix` で自動修正できますが、英字に隣接する数字（`600nm` など）と引数が続く関数名（`sin(x)`）は囲む範囲の判断を要するため報告だけが出ます。検査されるのは上の4ルールと表記揺れの辞書だけなので、ボールド体の禁止・`:Anki[]` の乱用・分類タグとフロントマターの整合などは `syntax-guide.md`・`math-notation-guide.md` の「仕上げチェックリスト」で writer が自分で確認します（**エラー0件は記法ルールを満たした証明にはなりません**）。

検査は2つのパスに分かれています（`lint:svx` は両方を走らせます）。**記法ルール**（`lint:svx:syntax`）は、ルール導入時点で違反を含んでいた既存記事を `app/.textlintignore` にベースラインとして列挙し、**新しく書く記事から適用**する運用です。ベースラインの記事を編集したときは検査されないため、writer は自分が書いた・直した箇所について記法ルールを守り、記事全体の既存の違反をついでに直そうとはしません（記事全体の数式・インラインコードの記法を整え、ベースラインから外すのは `format-math-notation` スキルの担当です）。一方**表記揺れ**（`lint:svx:notation`）はベースラインの対象外で、**常に全記事を検査します**（記事全体で表記が揃っていることに意味があり、直す手間も1語の置き換えで済むため）。

## author-style-analyzer（分析側）

### 役割

過去記事と編集履歴を分析し、著者固有の執筆スタイルを再利用可能なガイドとして整理します。単なる分析レポートではなく、文章生成・推敲で参照できる実用的なガイドを出力することが目的です。

### 参照ファイル（`references/`）

分析内容に応じて必要なものだけを読みます。

| ファイル | 役割 |
| --- | --- |
| `thinking-flow-analysis.md` | 思考プロセスの分析プロンプト（→ `thinking-flow.md`） |
| `writing-style-analysis.md` | 文章構成の分析プロンプト（→ `writing-style.md`） |
| `stylistic-quirks-analysis.md` | 表現上の癖の分析プロンプト（→ `stylistic-quirks.md`） |
| `refine-style-analysis.md` | Git履歴による修正傾向の分析プロンプト（→ `refine-style.md`） |
| `collaboration-workflow.md` | 実行方式（2モード・観点ごとのパイプライン）とエージェント間のレビュー観点 |
| `analysis-workflow.js` | 決定論オーケストレーションの正典スクリプト（Workflow ツールで実行） |
| `output-contract.md` | 統合側の共通規約：記述形式・ルールの基本形式・ルールID・根拠の記載方法・廃止と保留・禁止事項（`SYNTHESIS_CONTRACT` の出所。通常は全文を読まない） |
| `output-contract-<観点>.md` | 観点別の規約4ファイル：扱う対象・追加できる項目・必須要素・根拠の書き方（統合エージェントは自分の担当観点の1本だけを Read） |
| `output-contract-analysis.md` | 分析側の判断基準：確度・事実と推測・記事タイプ・反例・一般化（`ANALYSIS_CRITERIA` の出所。通常は全文を読まない） |

### 基本ワークフロー

1. 分析対象を確認する（参照する過去記事の選定、タスクリスト上の分析済み・未分析の別、既存ガイドの有無、Git履歴の利用可否など）
2. 記事を分類する（概念解説・実装解説・チュートリアル・比較・導入・エッセイ など）
3. 各観点を独立して分析する（他の結論に引っ張られないよう独立実施）
4. 反証・境界レビューする（一般化のしすぎ、重複、反例の見落としを反証エージェントが検証）
5. 根拠と確度を整理する（確度・支持記事の slug と該当箇所・記事タイプ・反例・事実か推測か）。**ガイド本体に書くのは確度ラベル3語だけ**で、残りは `evidence/` へ。あわせて、根拠の散文に埋もれた実行可能な制約（適用範囲の線引き、頻度・分量の上限、記事タイプによる出入り）をルール欄・適用条件欄へ引き上げる
6. 既存ガイド本体・根拠インデックス・保留プールを差分更新する（既存内容を読んで保持し、上書きせず加筆・修正・調整で反映。補強・条件追加・例外追加・確度の再評価のほか、`evidence/` への記事ブロック追加、再分析記事のブロック書き直し、保留プールへの追記、根拠が増えた保留の本体への昇格に分類）
7. 文体解析タスクリストを更新する（実際に分析した記事の行を `[x]` にする。draft で除外した記事はチェックしない。該当行を一意に特定できないときは更新せずユーザーへ確認する）

このうち3〜6の実行のしかたはモードで変わります（後述の「2つのモード」）。1〜2の前処理と7の後処理はメインセッションが担います。

### 参照する過去記事の指定

分析対象とする過去記事は、引数（任意）で切り替えます。引数は**カンマ区切りのリスト**で、各要素は「`#id`（先頭が `#`）」または「slug」のいずれか。`#id` と slug は混在させて複数指定できます。引数なしも可。

- **要素が `#id` の場合** … `app/src/lib/content-pages` 配下の YAML から該当 id（色記事はカテゴリ、CG記事はセクションの識別子）を探し、その配下の実装済み記事（`slug:` を持つ `PageLink`）を対象にする
- **要素が slug の場合** … その slug に対応する記事（`app/src/routes/{cg,color-theory,color-fields}/**/<slug>/+page.svx`）を参照する
- **複数要素・混在の場合** … `#id` と slug をカンマ区切りで自由に混在できる。各要素をそれぞれ上記の規則で解決し、得られた記事をすべて統合して分析対象にする（同じ記事が複数要素から得られたら1本にまとめる）。見つからない要素は報告し、残りの要素で続行する
- **引数なしの場合** … **全記事の一括分析は既定で行わない**。解説記事は多数（数十〜100本超）あり、一度に分析するとフリーズの原因になるため、分析前にまず `#id` や slug（カンマ区切りで複数・混在可）で範囲を指定するよう求める（このとき、タスクリストで `[ ]` が残っているスコープを候補として提示する）。どうしても全記事を対象にする場合は、ユーザーの明示同意のうえで **`#id`（カテゴリ／セクション）単位のチャンクに分割して順に実行**し、各チャンクの結果を既存ガイドへ差分更新として積み上げる
- **「まだ分析していない記事」など自然言語で指定された場合** … タスクリストの `[ ]` 行から対象を解決し、解決したルート一覧（件数つき）を提示して承認を得てから分析に進む

いずれの場合も、フロントマターが `draft: true` の記事は除外します。指定スコープに分析済み（`[x]`）の記事が含まれるときは、再分析するか未分析だけに絞るかをユーザーへ確認します（公開後に加筆・推敲された記事は再分析に意味があるため、機械的な除外もしません）。判断材料として、`evidence/<観点>/<slug>.md` の `分析時点` と記事の現在の最終更新コミットを比べ、本文差分の有無を添えて確認します。

### Git履歴の解釈規則

`refine-style.md` を作成・更新するときは、Gitのコミット履歴を使います。コミットメッセージを次の規則で解釈します。

- `記事タイトル` と `[ai-draft]` の両方を含むコミット … AIによる生成（修正前）。`記事タイトル [ai-draft]`（主に初稿）や `記事タイトル：修正内容 [ai-draft]`（初稿ではないが、AIによる修正しか含まない場合）が該当する。後方互換のため、従来の `記事タイトル：草稿を書かせた` も同じ扱いとする
- 同じ `記事タイトル` で始まり、`[ai-draft]` を含まないその後のコミット … 人間による編集（修正後）

同一記事のコミットを時系列で追い、AI草稿から人間の編集までの差分を分析します。誤字修正・メタデータ変更・リンク更新・フォーマット整形・技術的誤りの訂正などは、文体上の修正から分離します。

### マルチAgentによる役割分担（Workflow 実行）

`collaboration-workflow.md` に従い、観点ごとの Agent が独立分析と反証レビューを行います。目的は「意見交換」ではなく、異なる観点から独立した仮説を立て、互いの根拠・一般化・分類・適用条件を検証して単独分析の偏りを減らすことです。

実行方式は **Workflow ツールによる決定論的オーケストレーション**です（正典スクリプト：`references/analysis-workflow.js`）。以前は Agent Teams（teammates）で実行していましたが、teammate 同士のピア通信が「A が B のメッセージ待ち、B が A 待ち」というアイドル・デッドロックでハングしやすく（1時間経っても終わらない）、進行役の LLM がフェーズ受け渡しをポーリングし損ねても止まるため、Workflow ベースへ移行しました。制御フローが JS のためハングせず、各エージェントは構造化出力（スキーマ準拠 JSON）を返して終了します（idle プロセスが残らないので `TaskStop` は不要）。teammate 方式の「Agent 同士が直接議論して反証する」性質は、**独立エージェントが他エージェントの構造化出力を反証検証する**形に置き換えています（自由討論は失うが、反証の実効はスキーマ＋反証エージェントで担保）。

実行の流れ：

1. **前処理（メインセッション）** … タスクリストで分析状況を確認しつつスコープを解決し、対象記事の絶対パス・slug・記事タイプ・分析時点のコミット短縮SHA・再分析かどうか・除外記事・Git 履歴を使える記事・既存ガイド本体の出力先・根拠インデックス `evidence/` と保留プール `pending/` のパス（後者2つは省略可、省略時はスクリプトが導出）・参照プロンプトのパスを整理して **manifest**（`analysis-workflow.js` 冒頭コメントに定義）を組み立てる。SHA はメインセッションが `git log` で確定させ、エージェントに推測させない
2. **Workflow 実行** … `Workflow({ scriptPath: '.claude/skills/author-style-analyzer/references/analysis-workflow.js', args: <manifest> })` を呼ぶ
   - manifest はJSONオブジェクトとして渡すが、実行環境によっては `args` がJSON文字列としてスクリプトに届く。正典スクリプト冒頭は `const m = typeof args === 'string' ? JSON.parse(args) : args` として両対応済み（この防御がないと `m.targets` が `undefined` になり `m.targets.map` で即失敗する）。
3. **後処理（メインセッション）** … 戻り値と実ファイルで更新を検証し、`scripts/` のスクリプトを走らせ、タスクリストの該当行を `[x]` へ更新して、ユーザーへ変更内容を報告する。**根拠モードでは、戻り値を `style-evidence-write.mjs` へ渡して根拠を機械生成するのがこの段階の仕事**になる（Workflow スクリプトはファイルシステムへ触れないため）

Workflow を使えない場合は、サブエージェント（`Agent` ツール）を同じ順で呼ぶか、単一エージェントの役割切り替えで代替できます（反証の忠実度は下がります）。

| 役割（スクリプト内のエージェント） | 担当 |
| --- | --- |
| Thinking Flow Analyst | 執筆時の問題認識・判断・説明戦略・読者モデル・思考の遷移を復元（→ thinking-flow）（Opus / effort high） |
| Writing Structure Analyst | 記事・セクション・段落・文の構成傾向を分析（→ writing-style）（Opus / effort high） |
| Stylistic Quirks Analyst | 語彙・言い回し・文末・接続・表記の癖を分析（→ stylistic-quirks）（Opus / effort high） |
| Revision Diff Analyst | Git履歴からAI草稿と人間編集の差分を分析（→ refine-style）（Opus / effort high）。**Git 履歴が無い回は起動しない** |
| Evidence Reviewer | 根拠の強さ（記事数・引用対応・反例・偏り）を反証検証（sonnet / effort medium）。根拠モードでは過剰な当てはめを弾くのが主眼 |
| Boundary Reviewer | 書き上がった本体4ファイルを読み、責務と境界（重複・混同）を横断検証（sonnet / effort high — 配置判定が微妙なため思考量を上げる）。**発見モードのみ・検出だけで修正しない** |
| Synthesis Editor（観点単位） | 反証の結果を反映し、担当観点の本体＋evidence＋pending を差分 Edit で更新（Opus / effort high）。**発見モードのみ** |

根拠モードでは、4アナリストが「発見」ではなく「判定」を担います（Rule Judgment Analyst）。渡されるのはルールID一覧と記事本文だけで、ガイド本体・`evidence/`・`pending/` は読みません。

Coordinator の進行管理は JS の制御フローが担うため、独立した役割としては置きません。

### 2つのモード

分析には目的の異なる2種類があります。「新しく書いた記事が既存ルールを支持するか記録する」ことと「溜まった根拠からガイドを見直す」ことは別の作業なので、手順を分けています。

| | 根拠モード（既定） | 発見モード（`--discover`） |
| --- | --- | --- |
| 目的 | 記事が既存ルールを支持するかを記録する | ガイドそのものを見直す |
| 書き込み権限 | `evidence/` のみ | 本体・`evidence/`・`pending/` の3層 |
| 新しい特徴 | `pending/` へ1行で積む | 昇格・採用を判断する |
| ガイド本体 | 触らない（確度の降格を除く） | 更新する |
| 境界レビュー | 実行しない | 実行する |
| 記事指定なしの実行 | 不可 | 可（棚卸しのみ） |
| エージェント数 | 8体（Git 履歴なしなら6体） | 10〜13体 |

```
/author-style-analyzer #cg-basics              → 根拠モード（既定）
/author-style-analyzer #cg-basics --discover   → 発見モード
/author-style-analyzer --discover              → 棚卸しのみ（記事を読まない）
```

**根拠モードでも発見は起きます。** 変わるのは「見つけた特徴をその場で本体に入れるかどうか」だけで、根拠モードでは `pending/` に積み、採用の判断を発見モードへまとめます。発見の機会を捨てる設計ではありません。

棚卸しのみの回（`targets` が空）では、分析・反証を流さず統合と境界レビューだけを行います。この回はどの Analyst も起動しないため、`gitAnalyzable` が空でも refine-style の系列を除外しません（除外すると refine-style の保留プールだけが棚卸しから漏れます）。

manifest の `freezeConfidence: true` を渡すと、統合エージェントは**既存ルールの `確度` 欄を書き換えません**。昇格候補の採否と保留プールの整理だけを行い、支持記事数の集計は昇格候補の裏取りに使う参考値として渡されます。移行で作られた根拠は意図的に不完全なので、支持記事数だけを見て機械的に降格させると、根拠が未展開なだけのルールまで弱い傾向へ落ちるためです（根拠モード側で `--round` が担っている絞り込みに相当します）。

進行は**観点ごとのパイプライン**で流します。4観点を揃える必要があるのは境界レビューだけなので、これを統合の後段へ移し、`分析→反証→統合` を観点ごとに独立して流します。

```
根拠モード：  観点ごとに 判定 → 反証         → [後処理でスクリプトが記帳・集計]
発見モード：  観点ごとに 分析 → 反証 → 統合  → 境界レビュー（唯一のバリア）
```

### スクリプト（`scripts/`）

前処理・記帳・集計はスクリプトが担います。**エージェントに数えさせない・書かせないための分担**です。Workflow スクリプトはファイルシステムへ触れないため、いずれもメインセッションが Bash で実行します。

| スクリプト | 用途 |
| --- | --- |
| `style-rule-ids.mjs --list` | ルールID一覧を作る（判定エージェントへ渡す唯一のガイド情報。約1万字） |
| `style-rule-ids.mjs --write` | 新しく追加されたルールへ ID を採番する（欠番を維持し、再利用しない） |
| `style-evidence-write.mjs <json> --write` | 判定結果から `evidence/<観点>/<slug>.md` を機械生成する（根拠モードの記帳） |
| `style-evidence-tally.mjs` | 支持記事数を集計し、確度の降格候補・廃止候補・ID不整合を検出する |
| `style-pending-promote.mjs` | 保留プールから昇格候補（支持3記事以上）を抽出する |
| `style-guide-size.mjs [--ref <rev>]` | writer が読むガイドの肥大化率（文字数の増減%・ルール数）を測る |
| `style-evidence-split.mjs` | 移行専用（1回きり）。集約形式の根拠を記事単位へ分割する |

`style-guide-lib.mjs` が共通の読み書き規約（ルールID の抽出、根拠ファイルの解析・整形、支持記事数の集計）を持ちます。

#### 実行コストの最適化

大量トークン・長時間実行を避けるため、`analysis-workflow.js` は次の設計を採る。

- **根拠モードの統合ステージを廃止**（最大の効き目）… 記事が既存ルールを支持するかの記録は、判定結果から根拠ファイルへの機械的な写像に落ちる。ここに統合エージェントを置くと、自観点の「ガイド本体・根拠・保留」を全文 Read することになり、4体合計で48万字級の読み込みが発生していた。判定結果を構造化出力で返し、スクリプトが書く形にすればこれが消える。副次的に「ガイド本体を読ませない」も成立する（`Edit` は同一会話で Read 済みのファイルしか編集できないため、エージェントに書かせるなら本体を読ませることになる。記事単位の新規 Write に落とせばその必要がない）。
- **根拠を記事単位のファイルへ分割**… 集約1ファイルだと、1記事の分析でも観点あたり4万字級の全文 Read が必要だった。記事1本1ファイルにすれば、触る記事のファイルだけを読み書きすれば済む。あわせてルール名をルールIDへ置き換えたことで、根拠の総量も 174,970字 → 128,549字（−27%）に縮んだ。
- **エージェント数と読み込み量を記事数に追随させる**… 以前は記事1本を分析する回でも13体が起動し、同じ量を読んでいた。`gitAnalyzable` が空の回は Revision Diff Analyst の系列を起動しない（旧版は Git 履歴が1本も無くても起動し、「確認できないと明記する」だけの仕事をさせていた）。ただし棚卸しのみの回はこの除外を当てない（どの Analyst も起動しないので、当てると refine-style の保留プールだけが棚卸しから漏れる）。棚卸しのみの回は統合4体＋境界レビュー1体の計5体で済む。
- **境界レビューを後段へ移してパイプライン化**… 旧版は「4分析が全部終わる→反証が全部終わる→統合」の3段バリアだった。4観点を揃える必要があるのは境界レビューだけなので、これを統合の後段へ移すと、壁時計が「各段の最遅の合計」から「最も遅い観点の合計」へ縮む。代償として、重複・配置誤りの解決が「書く前の事前回避」から「書いた後の事後検出」に変わる（検出結果は報告し、修正は次のラウンドまたは個別の指示で行う）。
- **反証・境界レビューの軽量ティア**… 構造化 JSON の検証が主で上位モデルの上積みが小さいため、`model: 'sonnet'` で実行する。Evidence Reviewer（チェックリスト的検証）は `effort: 'medium'`。Boundary Reviewer（4ファイル横断で上位ルールと下位表現の分離・配置を判定する）は判断がより微妙なため `effort: 'high'`（モデルを上げるより思考量を上げる方が費用対効果が高い）。
- **重いステージの effort 固定**… 判断の重い分析・判定と統合は、モデルは上位ティア（Opus）を保ちつつ `effort: 'high'` を明示する。セッションを `xhigh` などに引き上げると重いエージェントの思考量が一斉に跳ね上がって実行コストと時間が膨らむため、スキル内の思考量をセッション設定から切り離して安定させている。
- **契約の要約をプロンプトへインライン展開**… 分析・反証エージェントは `ANALYSIS_CRITERIA`、統合エージェントは `SYNTHESIS_CONTRACT` に従い、契約ファイルの全文を Read しない。統合4体が `output-contract.md` の全文を読むと、それだけで約5.5万字を消費する。判断がつかない節だけを正典から読む。要約と正典が食い違った場合は正典を正とし、要約を直す。
- **契約ファイルの対象読者別・観点別分割**… 契約は共通規約 `output-contract.md`、観点別4ファイル `output-contract-<観点>.md`、分析側 `output-contract-analysis.md` に分けている。統合エージェントは共通規約の要約＋自観点の1ファイルだけを読み、他観点の契約は読まない。Markdown 原本のまま分けているため JS 文字列化のドリフトは生じない。
- **集計をエージェントに数えさせない**… 支持記事数と保留の昇格候補は、メインセッションがスクリプトで求めて manifest へ入れる。これがないと統合エージェントが `evidence/` 全体と `pending/` 全文を読み直すことになる。

## author-style-writer（利用側）

### 役割

分析済みのガイドを参照し、著者らしい文章を生成・レビュー・推敲・編集します。このスキルはスタイル分析を行いません。ガイドに根拠のない特徴を、著者の癖として推測で追加しません。

### モードと読み込み範囲

依頼内容と引数から次のモードを判断し、モードに応じたガイドだけを読みます（4ガイドの合計が大きいため、必要な範囲に絞る）。`syntax-guide.md` と `math-notation-guide.md` は全モードで必須です。

| モード | 読むガイド（`syntax-guide.md`・`math-notation-guide.md` に加えて） | 用途 |
| --- | --- | --- |
| 生成 | 4ガイド全部 | 白紙・雛形・書きかけのファイルに本文を新しく書く |
| 推敲 | `writing-style.md`・`stylistic-quirks.md`・`refine-style.md` | 既存文章を依頼された範囲で見直す |
| 編集 | `refine-style.md`・`stylistic-quirks.md` | すでに本文のある記事ページに指示された編集を加える |
| 編集・最小 | `refine-style.md` | 語句の言い換え・文末の調整・表記の統一など、構成に触らない局所的な修正 |

- **生成モードでは全部読みます。** 記事を書き起こす作業では、何を説明するかの判断（`thinking-flow.md`）から表現（`stylistic-quirks.md`）まで全層が必要なので、読み込み量を削るより品質を優先します。
- 推敲モードで `thinking-flow.md` を読まないのは、説明する内容そのものを設計し直さないからです。編集モードではさらに `writing-style.md` も外します（構成に触らないため）。
- **範囲は途中で広げてよい**運用です。節の追加・並べ替えが必要になったら `writing-style.md`、新しい説明を書き足す必要が出たら `thinking-flow.md` をその時点で読みます。足りないまま推測で補いません。

### 参照するガイドと優先順位

本文を書く前に、まず `writing-guides/syntax-guide.md` と `writing-guides/math-notation-guide.md`（必須の記法・書式）を読み、そのうえでモードに応じた文体ガイドを読みます。ガイド同士が競合する場合は、次の順序で判断します。

1. ユーザーが今回明示した指示
2. 記事の目的と技術的な正確さ
3. `writing-guides/syntax-guide.md`・`writing-guides/math-notation-guide.md`（記法・書式の必須ルール）
4. `thinking-flow.md`
5. `writing-style.md`
6. `refine-style.md`
7. `stylistic-quirks.md`

表面的な言い回しよりも、説明の目的・思考の流れ・情報構成を優先します。記法・書式の必須ルールは、文体上の癖より優先されます。この順序はそのモードで読んだガイドの間で適用し、読んでいないガイドの内容を推測で補って持ち込みません。

各ガイド内では、ルールに付いた `確度` ラベルで当てる強さを判断します（`強い傾向` は既定で当て、`条件付きの傾向` は適用条件に合うときだけ、`弱い傾向` は積極的に適用しない）。確度の根拠はガイドに書かれていませんが、それを確認しに行かず、ラベルをそのまま使います。

`writing-guides/pending/` と `writing-guides/evidence/` は**読みません**。前者は主要ルールに満たない保留観察、後者は各ルールを支持する記事の索引で、どちらも分析専用の補助記録です。とくに `evidence/` は個別記事の該当箇所が記事名つきで並んでいるため、読むと**特定の記事の言い回しをなぞる**ことになり、「個別記事を文体の手本にしない」原則に正面から違反します。ルールの根拠を確かめたくなっても開きません。

文体の出所はこれらのガイドだけです。個別の記事ファイル（他の `+page.svx`）を文体・構成・言い回しの手本として読みません。ガイドは複数記事の「共通して言える傾向」を抽象化したもので、個別記事の具体的な言い回しは意図的に落としてあります。個別記事を手本にすると抽象化を飛び越えて特定の一文を持ち込み、どの記事も似通ってしまうためです。他の記事ファイルを開いてよいのは**相互リンクのため**（実在・パス／slug・タイトル・扱う範囲の確認）に限り、そこで目にした導入文・説明文を単語だけ差し替えて流用しません。導入文のように `syntax-guide.md` でテイストが定まるものも、規約に従って今回の記事のためにその都度書き起こします。自己検査として、名詞を入れ替えるだけで別記事に通用する文は流用とみなして書き直します（SKILL.md「他の記事ファイルは文体の手本にしない」「流用チェック」）。

### ワークフロー（生成モード）

1. 執筆条件を整理する（目的・読者・前提知識・記事タイプ・範囲・長さ など）
2. 思考の流れを設計する（`thinking-flow.md`、生成モードのみ）
3. 構成を作る（`writing-style.md`、生成・推敲モード）
4. 初稿を書く（表現の模倣より内容と説明の流れを優先。他の記事ファイルの文章をなぞらず、導入文もこの記事のために書き起こす。`keywords` 引数が与えられていれば、そのすべてを本文中で必ず取り上げる）
5. 表現を調整する（`stylistic-quirks.md`、生成・推敲・編集モード。最終的な語り口の調整のみ）
6. 推敲する（`refine-style.md`、全モード。AI草稿に対して著者が行いやすい修正を適用）
7. スタイルレビューを行う（Thinking Flow / Writing Style / Stylistic Quirks / Refine Style の4観点。推敲・編集モードでは、そのモードで読んだガイドに対応する観点だけを使う）
8. 記法・表記チェックを通す（`npm run lint:svx` のエラーを解消し、textlint が見ない項目は `syntax-guide.md`・`math-notation-guide.md` の「仕上げチェックリスト」で確認する）

### 既存コンテンツの扱い

対象ファイルにすでに中身がある場合は、書き始める前に**執筆対象箇所**（箇条書きメモ・`TODO`・`:::Add`・`:::Delete`・`:::Fix{target="text"}`・`::ComingSoon`）と**執筆済み箇所**（敬体の文章）を仕分けます。執筆済みの文章と見出し構成（テキスト・順序・分類タグ）は変更せず、執筆対象箇所だけを埋め、もとのメモは HTMLコメントで残します。

`:::Add`（加筆したい箇所）・`:::Delete`（消す予定の記述）・`:::Fix{target="text"}`（既存の記述への修正指示）は著者が残した指示なので、指示どおりに本文へ反映し、**ブロックごと削除します**（HTMLコメントで残さない。`:::Delete` は囲まれた記述もろとも消します）。文章の加筆指示は `:::Add`、既存の記述の直しは `:::Fix` で書かれます。ただし `:::Fix{target="svg"}` / `{target="demo"}` は図版・デモへの修正指示なので、このスキルでは手を付けず残します（`/svg-diagram-component`・`/add-threejs-demo` の領域）。`:::Pending`（採否を検討中の文章）も、中身が指示ではなく候補の本文そのもので採否は著者が決めるため、**ブロックも中身もそのまま残します**。

ただし**図版・デモの TODO**（「〜のグラフ」「〜の図解」のように、本文ではなく図を入れたい旨のメモ）はこの対象外です。本文の文章で埋めず、`:::Todo` ディレクティブに書き換えて図が入るべき位置に残します（HTMLコメントや地の文の `TODO：〜` のままにしない）。記法は `syntax-guide.md` ルール4に従い、図が無い間はデモコンポーネントを `import` しません。

ただし、ページ内の見出しが **TODO 見出し1つだけ**（`## TODO`・`## :WithGradeTag[TODO]{grades="1"}` など）の場合は、維持すべき見出し構成ではなく `create-*-page` スキルが置いた雛形のプレースホルダなので、**見出し構成を自分で考えて決めます**（白紙でも、TODO 見出しの下に箇条書きメモがあっても同じ）。TODO 見出しは設計した見出しに置き換え、分類タグは `syntax-guide.md` に従って雛形の `grades` / `group` を出発点に振り分け、本文中の分類タグの和集合をフロントマターの分類と一致させます。見出しが2つ以上ある場合、または1つでもそれが TODO でない場合はこの例外にあたらず、見出し構成を維持します。すでに付いている `:Anki[]` / `:Mark[]` は、明示的な指示がない限り**絶対に勝手に外さず**、囲む語・範囲を保持します（`syntax-guide.md` ルール1の再掲・強調で、文体判断で外してよいものではない）。執筆対象が無い（全文が敬体で書き上がっている）場合は生成に進まず、「既存文章を推敲する場合」の手順へ切り替えます。

### 記事ページの編集（編集モード）

slug（＋編集指示）を渡すと、すでに本文のある記事ページ（`+page.svx`）に指示された編集を加える編集モードで動きます。読むガイドは `syntax-guide.md`＋`math-notation-guide.md`＋`refine-style.md`＋`stylistic-quirks.md`（構成に触らない局所的な修正なら `refine-style.md` だけ、構成変更を伴う指示なら `writing-style.md`、説明の書き足しを伴う指示なら `thinking-flow.md` も追加）。slug で対象ファイルを特定し、`syntax-guide.md`・`math-notation-guide.md` と読み込んだ文体ガイド（特に `refine-style.md`）に沿って編集し、**指示の直接対象だけでなく前後の文章・セクションの流れも整えて**報告します。編集対象でない敬体の既存文章と見出し構成は変更しません。既存の `:Anki[]` / `:Mark[]` も、編集指示が明示的に求めない限り**絶対に勝手に外さず**、文を書き換える場合も落としません（対象語が動くなら一緒に動かす）。実際の文章変更の規律は「既存文章を推敲する場合」と共通です。報告の前に `npm run lint:svx` を通します（→「機械的に検査できるルール」）。

### ガイドが空・不足している場合

ガイドが存在しない・内容が薄い・互いに矛盾している場合は、推測で著者の特徴を補いません。`evidence/` や個別記事を開いて埋め合わせることもしません。利用できるガイドの範囲だけを適用し、不明な点は一般的に自然な文章として処理し、強い模倣を避け、必要に応じて analyzer によるガイド更新が必要であることを示します。4つの文体ガイドが空の間も、`syntax-guide.md`・`math-notation-guide.md` の記法・書式ルールは常に適用されます。

なお、ルールに根拠が書かれていないのは「内容が薄い」ことではありません。根拠は分析専用の `evidence/` へ分離してあり、執筆に必要な情報はルール欄・適用条件欄・確度ラベルに揃っている前提です。

## ファイル構成

```
.claude/skills/
  ├─ author-style-analyzer/
  │    ├─ SKILL.md
  │    └─ references/
  │         ├─ thinking-flow-analysis.md
  │         ├─ writing-style-analysis.md
  │         ├─ stylistic-quirks-analysis.md
  │         ├─ refine-style-analysis.md
  │         ├─ collaboration-workflow.md
  │         ├─ analysis-workflow.js    … 決定論オーケストレーションの正典スクリプト
  │         ├─ output-contract.md          … 統合側の共通規約（SYNTHESIS_CONTRACT の出所）
  │         ├─ output-contract-thinking-flow.md    ┐
  │         ├─ output-contract-writing-style.md    │ 観点別の規約
  │         ├─ output-contract-stylistic-quirks.md │ （担当観点の1本だけを Read）
  │         ├─ output-contract-refine-style.md     ┘
  │         └─ output-contract-analysis.md … 分析側の判断基準（ANALYSIS_CRITERIA の出所）
  └─ author-style-writer/
       └─ SKILL.md

scripts/
  ├─ style-guide-lib.mjs         … 共通の読み書き規約（ID抽出・根拠の解析・集計）
  ├─ style-rule-ids.mjs          … ルールIDの採番と一覧出力
  ├─ style-evidence-write.mjs    … 判定結果から根拠を機械生成（根拠モードの記帳）
  ├─ style-evidence-tally.mjs    … 支持記事数の集計・降格候補・不整合の検出
  ├─ style-pending-promote.mjs   … 保留の昇格候補の抽出
  ├─ style-guide-size.mjs        … writer が読むガイドの肥大化率の測定
  └─ style-evidence-split.mjs    … 移行専用（集約形式 → 記事単位）

writing-guides/
  ├─ syntax-guide.md        … 記法・書式の必須ルール（writer が参照）
  ├─ math-notation-guide.md … 数式・インラインコードの記法（syntax-guide.md から切り出し）
  ├─ thinking-flow.md       ┐ ルール見出しに [TF-001] 形式のIDが付く
  ├─ writing-style.md       │ analyzer が生成 / writer が参照
  ├─ stylistic-quirks.md    │ （主要ルールのみ）
  ├─ refine-style.md        ┘
  ├─ evidence/              … 根拠（analyzer が読み書き / writer は読まない）
  │    └─ <観点>/           … 観点ごとのディレクトリ
  │         ├─ _README.md              … 凡例と観点固有の確度判定基準
  │         ├─ cg/basics/anti-aliasing.md   ┐ 記事1本1ファイル
  │         └─ color-theory/pccs-basics.md  ┘ パスがそのまま slug
  ├─ pending/               … 保留プール（analyzer が読み書き / writer は読まない）
  │    ├─ thinking-flow.md
  │    ├─ writing-style.md
  │    ├─ stylistic-quirks.md
  │    └─ refine-style.md
  └─ STYLE-ANALYSIS-TASKLIST.md
                            … 記事ごとの分析済みチェックリスト（analyzer が読み書き / writer は読まない）

app/
  ├─ textlint/
  │    ├─ README.md         … 記法・表記チェックの使い方・対象外・ベースライン運用
  │    ├─ rules/            … math-notation-guide.md の4ルールの実装（writer が npm run lint:svx で通す）
  │    ├─ lib/svx.js        … ディレクティブ・数式などを検査対象から外す共通処理
  │    └─ prh.yml           … 表記揺れの統一辞書（specs で自己テスト）
  ├─ .textlintrc.json       … 記法パスの設定（Markdown プラグインの extensions に .svx を追加）
  ├─ .textlintrc.notation.json
  │                         … 表記揺れパスの設定（prh の辞書を読む）
  └─ .textlintignore        … 記法パスのベースライン（ルール導入時点で違反を含む記事）
```

## 品質を担保する仕組み（output-contract）

契約は対象読者と観点で分かれています。統合（Markdown 生成）時の共通規約（記述形式・ルールの基本形式・ルールID・根拠の記載方法・廃止と保留・禁止事項）は `output-contract.md`、観点ごとの規約（扱う対象・追加できる項目・必須要素・根拠の書き方）は `output-contract-<観点>.md` の4ファイル、分析・反証時の判断基準（確度・事実と推測・記事タイプ・反例・一般化）は `output-contract-analysis.md` が定めます。

**エージェントはいずれの全文も通常は読みません。** 分析／反証は `ANALYSIS_CRITERIA`、統合は `SYNTHESIS_CONTRACT` というインライン要約に従い、判断がつかない節だけを正典から読みます（要約が食い違ったら正典を正とし、要約を直す）。統合エージェントが読む観点別ファイルは、自分の担当観点の1本だけです。主なポイントは次のとおりです。

- 観察結果ではなく、条件付きで実行できるルールとして記述する（「〜の場合は〜する。ただし〜には適用しない」）
- 主張の強さを根拠に合わせる。確度は **強い傾向 / 条件付きの傾向 / 弱い傾向** の3段階
- ルール見出しは `### [WS-012] ルール名`。基本形式は `確度`（先頭・ラベル3語のみ）／`対象`／`ルール`／`適用する状況`／`適用しない状況`／`使用量`／`変種`／`注意`／`関連ルール`。**`根拠` 欄と `反例・例外` 欄は置かない**（根拠は `evidence/`、一般化できる反例は `適用しない状況`、記事固有の反例は `evidence/` の `※` 注記へ）
- `使用量`（頻度・分量の上限）／`変種`（条件による現れ方の違い）／`注意`（運用上の注意）は、実行に必要な操作的制約の受け皿。「1段落に2回以上重ねない」のような制約を根拠の散文へ埋めない。逆に `使用目的` / `使用されやすい文脈` / `構成上の効果` / `前後の要素` は、ルール欄の言い換えになるなら書かない
- 事実（本文やGit差分から直接確認できる）と推測（リバースエンジニアリングによる仮説）を区別する
- 一般的な文章術・記事テーマ固有の専門用語・AI草稿由来の表現を、著者固有の癖として扱わない
- ガイド本体に分析対象そのものを書かない。記事名・シリーズ名・出現件数・分析を重ねた経緯を、「ガイドの目的」節・節の前書き・ルール欄・確度欄のいずれにも書かない（ガイド本体は「どの記事を分析したか」の記録ではなく再利用可能なガイド）。記事単位の記録は `evidence/<観点>/<slug>.md`、更新履歴は分析ログ側に保持する
- 根拠は `evidence/<観点>/<slug>.md` へ**ルールIDで**登録し（ルール名は書かない）、`分析時点` と `記事タイプ` を記録する。改稿された記事はそのファイルを丸ごと書き直し（記事単位の撤回）、支持記事が減ったルールは確度を再評価する
- 主要ルールに満たない保留はガイド本体に混ぜず、対応する保留プール `writing-guides/pending/<観点>.md` に分離する。根拠が増えた保留は本体へ昇格させ、pending から削除する（本体と保留プールで重複させない）
- 保留は1項目1行の3欄形式（`保留ID｜理由｜支持記事`）で書く。散文にすると昇格判定を機械化できない
- 既存ガイドは全面書き直しをせず差分更新する

## 現状メモ・今後の予定

- `writing-guides/` の4ガイド本体・`evidence/` 根拠インデックス・`pending/` 保留プールは、これまでの analyzer 実行で生成・更新されている（本体＝主要ルール、evidence＝採用済みルールの根拠、pending＝保留観察）
- `evidence/` は既存ガイドの根拠欄からの移行で作られたため、**初期値は不完全**である。「配色技法シリーズ10本すべて」のように記事を特定しない集約表現は展開しておらず、個別に名指しされた記事だけを登録している。`分析時点` の SHA も git 履歴から復元した値で、「その SHA の本文を読んで分析した」ことの保証ではない（ラウンド直前の一括修正コミットを指している記事がある）。各記事を再分析した時点で、その記事のブロックを正しい内容へ置き換える
- 4ガイド計は 162,604字（251ルール）。`spec/writing-guides-compaction.md` の受け入れ条件（132,231字以下）はまだ満たしていない。移行時点の 146,559字からの増加は、その後の分析ラウンドで追加されたルールとルールID の付与（約2,300字）による。ルール統合・補助項目の圧縮・上限値の見直しのいずれを採るかは判断待ち（`spec/COMPACTION-TASKLIST.md` フェーズ7）
- `pending/` の1行化は**未着手**。523項目が旧形式の散文のまま残っており、`style-pending-promote.mjs` の昇格候補の抽出対象に入らない。それまでは発見モードで散文を読んで判断する（経緯と設計は `spec/analyzer-cost-reduction.md`）
- `writing-guides/STYLE-ANALYSIS-TASKLIST.md` の初期状態は、これまでの analyzer 実行コミット（`writing-guides/` を触ったコミット）のスコープと、その時点で公開済み（非 draft）だった記事を突き合わせて復元したもの。以降は analyzer 実行のたびに更新する
- `writing-guides/syntax-guide.md` と `writing-guides/math-notation-guide.md`（数式の記法を切り出したもの）が記法・書式の必須ルールの正典。かつて存在した `write-content-draft` / `edit-content-draft` スキルとその `style-guide.md` は author-style-writer へ統合済みで、記事の草稿執筆・編集は author-style-writer が担う
