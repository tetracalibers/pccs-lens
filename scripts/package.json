{
  "name": "scripts",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "リポジトリの整合を保つメンテナンススクリプト置き場。app のビルドからは独立。",
  "scripts": {
    "sync-tasklists": "node sync-tasklists.mjs --check",
    "sync-tasklists:write": "node sync-tasklists.mjs --write",
    "format": "prettier --write ."
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
