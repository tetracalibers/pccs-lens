分析時点: `ade9282e`
記事タイプ: 概念解説

- RS-010（0abc1e1e→62689680：「有限の点としては表せません」を「通常の座標 (x/w, y/w) で位置を表せる点、すなわち原点から有限の距離にある点としては表せない、ということです」へ置き換え。38366e13→4d883afd：「なぜそうなるのかわからない」という指摘を受け、`kw` が `1` になるのは `k` が `1/w` のときだから、という導出の1段を明示している）
  ※ RS-010：どちらも「わかりづらい」という明示の依頼を受けた反応的な編集で、自発的な推敲でも同型が起きるかは確認できない
- RS-013（6d74a88c→4f609955：消点の定義を地の文から `:::Note` へ移し、地の文は無限遠点が有限の点へ写るという本筋だけにしている。74ec9e13→d3b4bce2：`w` が `0` の同次座標についての `:::Note` を、同値関係の節から該当する記述の直後にあたる定数倍の節へ移している）
- RS-014（74ec9e13→d3b4bce2：「実座標」→「通常の座標」、「ひとつの点」→「`1`つの点」、地の文の $$w^{\prime}$$ → `w'`。6d74a88c→4f609955：「移る」→「写る」。e9f06ea7→b68a238c：「一点」→「`1`点」。統一は複数コミットにわたって続いている）
  ※ RS-035：地の文の変数を $$...$$ からインラインコードへ戻す向きで、(c)（地の文の単一変数を数式へ）の反例にあたる
- RS-020（19a94905→33182246：草稿の「個別の変換の総まとめとなる節です。」を削除し、既習変換の列挙を箇条書きのリンクへ変えて導入を短縮している）
  ※ RS-020：de7b964b→b167a01a の記事分割では、露出した冒頭へ逆に導入2文を最小限で足しており、短縮は草稿が盛った導入を削る場面に限られる
- RS-021（74ec9e13→d3b4bce2：「同値」→「同次座標の同値関係」、「wの正規化」→「通常の座標への戻し方」。6d74a88c→4f609955：「発展：無限遠点と消点」を、その節の入口である平行線に合わせて「発展：平行線と無限遠点」へ改名し、消点の定義は `:::Note` へ落としている）
- RS-027（4a723d15→3e4aeb7c：「変換の比較まとめ」節の締め「個々の変換を別々の道具として覚えるのではなく…見通しよく理解できます」を節ごと削除。74ec9e13→d3b4bce2：「基本的な変換と同次座標で…と予告していた点にあたります」という後方参照も削除している）
- RS-029（4a723d15→3e4aeb7c：CardGrid で既出内容を再掲するまとめ節を、要約へ圧縮せず節ごと落としている）
  ※ RS-029：6d74a88c→4f609955 は公開へ向けた調整のコミットだが節の削除を伴わず、代わりに記事の意義を述べる締めの一文を足している（公開＝節削除ではない）
- RS-031（74ec9e13→d3b4bce2：ホモグラフィ行列と未執筆のイメージモザイキングに依存する「射影変換の用途」節を、段落ごと削除している）
- RS-032（59534d04→80d04fe2：デモで代替された図版の `<!-- TODO: … -->` 3件をまとめて削除し、`:::Todo` 相当のプレースホルダを実コンポーネントへ置き換えた状態に合わせている）
- RS-038（4d883afd→e9f06ea7：節末にまとめてあった1つの `:::Action` を、定数倍と直線の関係・交点が動かないこと・`w` を変えた場合の3箇所へ割り振り、パラメータ名を `:Mark[]` で囲み、冒頭にデモへの誘導だけの `:::Action` を1つ追加している。38366e13→4d883afd：`:::Action` を `w` と `k` に絞り、節の主題と関係の薄い `x`・`y` の項目を削除。e9f06ea7→b68a238c：デモに出ているのに本文が触れていない要素を語で名指しする形へ改め、色で対象を指す書き方をやめている）
  ※ RS-038：e9f06ea7 で足した「デモまで下にスクロールしてね！」は観察点を持たない誘導専用の `:::Action` で、着眼点1つという型からは外れる
