分析時点: `de7b964b`
記事タイプ: 概念解説

- RS-001（dd35dc94→27d7cf5c：公開コミットで frontmatter の `draft: true` を削除している）
  ※ RS-001：この記事は執筆メモをHTMLコメントで本文に残す書き方をとっておらず、メモの一括削除は生じていない。公開コミットに同居したのは、未作成ページ参照を含む節の削除である
- RS-010（1b523f7e→41485a54：「像の縮みも引き受けます」「奥行きを落とすだけ」が何を指すか分かりづらいという依頼を受け、断面をそろえる割合が奥行きに反比例することと、奥行きを落とす処理が必要な理由へ段階分けしている。59e9184e→1b523f7e：「奥にあるものほど強く中心へ寄せる」を、`w`・`h`・`d` を置いた式まで下ろしている）
  ※ RS-010：1b523f7e で足した数式による導出は 41485a54→dd35dc94 で数式を使わない言葉の説明へ差し戻されており、噛み砕く手段は数式に固定されない
- RS-013（108fb91c→25f7ba12：広角レンズ・望遠レンズとの対応を `:::Note` へ。b735d393→2e6d286b：視点より後ろの点でも像を持つという例外を `:::Note` へ。41485a54→dd35dc94：奥行きを最後まで残す理由を `:::Note` へ）
  ※ RS-013：25f7ba12→908ef975 では「投影中心は光が集まる点であり、眼やカメラの視点として捉えることができます」を `:::Note` から地の文へ戻している（後続の画角の説明が前提にする位置づけだったため）。退避は一方向の操作ではない
- RS-014（25f7ba12→908ef975：「投影線」→「投射線」を本文3箇所と Three.js 掲載コードのコメント2箇所で統一し、同じコミットで Todo メモの「75°」を本文化する際に「`75`度」へ改めている）
- RS-020（108fb91c→25f7ba12：草稿の3文リードから「ここでは、透視投影と平行投影のどちらにも共通する、投影の進め方を見ていきます」を削って2文へ。41485a54→dd35dc94：「投影中心と投影面が決まれば」という前提の言い直しと投影中心へのリンクも外して1文へ）
  ※ RS-020：dd35dc94 では導入から外した平行投影のリンクを、その語が主題として初出する「平行投影のウィンドウと画角」節の第1文へ付け替えている
- RS-021（41485a54→dd35dc94：「透視投影の変換」を「透視投影の正規化」と「正規化ビューボリュームの透視投影」の2小節へ分割・改名し、「クリッピング」も ### 見出しとして切り出している。b735d393→2e6d286b：ビューボリューム節に混ざっていた前方・後方クリッピング面の説明を「クリッピング面」の h2 として独立させている。108fb91c→25f7ba12：ウィンドウ節を「透視投影のウィンドウと画角」「平行投影のウィンドウと画角」へ分割）
  ※ RS-021：「ウィンドウ」「ビューボリューム」「正規化ビューボリューム」のように対象名だけの h2 はそのまま維持されており、操作が読める名へそろえるのは下位の ### 側にとどまる
- RS-031（dd35dc94→27d7cf5c：公開コミットで、未作成ページへの `<!-- TODO: … -->` を含む「投影座標系」節をまるごと削除している。b735d393→2e6d286b：参照先が実在するようになったクリッピングについては、`<!-- TODO: …（ページ未作成） -->` を外して本文の記述を実リンクへ置き換えている）
  ※ RS-031：同じ記事で削除とリンク化の両方が起きており、判断は参照先ページが実在するかどうかで分かれている
- RS-032（908ef975→b735d393：ウィンドウと画角の `:::Todo` プレースホルダを実 Three.js デモへ置き換え。f7d3beb8→59e9184e：透視投影の変換の `:::Todo` も同様に実デモへ置き換えている）
  ※ RS-032：`:::Todo` は図版の場所確保だけでなく、`:::Action` の文面や言い回しの候補を並べる申し送りにも使われ、後続コミットで本文へ書き下されている
- RS-038（41485a54→dd35dc94：`:::Todo` のままだった `:::Action` 2箇所に観察の観点を書き入れ、ビューボリューム側は「次の点に着目して、デモを観察してみよう」＋箇条書き3項目にしている）
  ※ RS-038：b735d393→2e6d286b では先に `:::Action` の枠だけを `:::Todo` で置き、文面は後続コミットで埋めており、分割・絞り込みと同時に行われるわけではない
