分析時点: `684976f6`
記事タイプ: 未記録

- RS-001（dc5e74e7→c2b2a08d）
  ※ メモを持たない記事。`draft: true` の除去のみを支持する
- RS-002（c2b2a08d：`:WithGroupTag` の group 値を `{['ImgP']}` → `{['CG','ImgP']}` へ訂正）
- RS-027（dc5e74e7→c2b2a08d／684976f6：他ページへの予告・留保4箇所を削除し、A/D変換節の予告は `:::Note`＋リンクへ置き換え。全体構成節末の意義づけの総括を削除）
  ※ 削られるのは留保・予告の言い回しであってリンクではない
- RS-013（dc5e74e7→c2b2a08d／684976f6：未導入のF値・シャッタ速度に依存する `:::Example` を2つとも削除し、A/D変換の関連ページ参照を `:::Note` へ移す）
  ※ 削除の判断基準は差分に明記がなく推測
- RS-020（dc5e74e7→c2b2a08d：既習ページ参照と問いかけを含む3文を、装置の位置づけと機種差を切り落とす2文へ差し替え）
- RS-033（c2b2a08d／684976f6：レンズ／絞り／撮像素子／A/D変換器／A/D変換）
- RS-037（684976f6：撮像素子の説明を電荷の発生から定義的な説明へ、シャッタを必要性から入る説明へ全面改稿）
  ※ 箇条書き→散文（純粋な列挙は箇条書きのまま残す）は逆方向の例（光の流れの5部品を節後半の素の箇条書きから節前半の番号付きリストへ前倒し）
