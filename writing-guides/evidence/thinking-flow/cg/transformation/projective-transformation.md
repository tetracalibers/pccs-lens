分析時点: `ade9282e`
記事タイプ: 概念解説

- TF-001（リード「アフィン変換は、同次座標行列の最下行が(0 0 1)に固定された変換でした。この固定を外し…」／「同次座標の一般形」節の第1〜2文「点をwが1の同次座標で表してきました。しかし…wの値は1に限らず」／「射影変換」節「アフィン変換では、行列の最下行を(0 0 1)に固定していましたが、射影変換ではこの最下行を一般の値にまで広げます」）
  ※ 記事冒頭・各節冒頭で同じ手を反復し、固定条件の解除というパラメータ操作の言い方をとる
- TF-061（「通常の座標への戻し方」節第1文「変換後のwが1以外の値をとるため、変換後の同次座標をそのまま通常の座標として扱うことはできません。そこで…」／「発展：平行線と無限遠点」節「wで割って通常の座標へ戻そうとすると0による除算になってしまい、通常の座標を計算できません」）
  ※ 「射影変換で保たれる性質」節は破綻を置かず性質の得失を述べるだけで進む
- TF-046（「定数倍しても同じ点を表す」節「この関係は、wを3本目の軸にとった空間で見ると、直線と平面の関係として捉えられます」に続けて、定数倍の集合＝原点を通る直線、kw=1 となる点＝w=1 平面との交点と順に対応づける）
- TF-038（「射影変換」節：w' = px+qy+r が点の位置で変わる→「点ごとに異なる縮め方」→「遠くのものほど小さく見える遠近感」／記事末尾「『平行な線がどこかで交わる』という、一見不思議な現象が起こります。そして、この現象は現実世界での遠くのものの見え方と一致するため…」）
  ※ 抽象→操作的な意味→現実の見え方という段の順序は記事内で一定せず、「射影変換で保たれる性質」節は性質→現実の見え方→効果の順で入れ替わる
- TF-041（「射影変換」節「アフィン変換では最下行の(p q r)が(0 0 1)だったので、w' = 0·x + 0·y + 1 = 1 となり、w'は常に1に保たれていました」／「発展：平行線と無限遠点」節で最下行が(0 0 1)なら無限遠点のwは0のまま保たれることを確認／平行線デモの観察指示「pとqどちらも0なら、格子は変形しない（最下行が(0 0 1)のアフィン変換）」）
  ※ 同一の特殊化を本文2箇所＋観察指示1箇所で3回繰り返しており、使用量の上限にあたる多い側の例
- TF-026（「同次座標の同値関係」節末「この見方は、wが実際に点ごとに変化する次の射影変換で効いてきます」→「通常の座標への戻し方」節「同次座標の一般形で解説した正規化を使って…w'で割ることで通常の座標へ戻します」と節名を挙げて受け直す）
- TF-053（「定数倍しても同じ点を表す」節：定数倍とwだけの変更を「指す点そのものが変わるか」という同一の問いで対置し、「定数倍ではxとyもwと同じ倍率で変わるため…しかし、wだけを変えた場合はxとyが変わらないため」と差が生じる理由まで添える／wで割る操作と「正規化」という命名の切り分け）
- TF-057（HomogeneousLineDemo・平行線デモの直前および周辺に観察指示を置き、動かす対象と着眼点を明示する。デモ本体より前の最初のブロックで「デモまで下にスクロールしてね！」と居場所を知らせる）
  ※ TF-060：暗記対象のマークは :Anki[直線性]・:Anki[平行性]・:Anki[定数倍] のように概念語中心で、取り違えると命題が反転する一語の切り出しは薄い
