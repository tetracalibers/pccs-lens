分析時点: `1557e364`
記事タイプ: 概念解説

- TF-001（第1節「1つの座標系ですべてを引き受けようとすると、どの段階から見ても扱いにくくなります。そこで、目的に合った座標系をいくつも用意し…この一連の処理がビューイングパイプラインです」）
- TF-008（「写る位置を決めるのはカメラ（視点）から見た対象の位置であって、仮想世界の原点から測ったワールド座標系の座標は、それを直接には表していないからです」など、各変換の必要性を理由まで下ろす）
- TF-013（「装置による違いを持ち込まずに値を揃えてあるので、正規化とついた名称になっています」／「視点を基準にとることから視点座標系とも呼ばれます」）
- TF-018（「z軸の反転は、z座標の符号を変えるだけの操作です。…z方向の倍率だけを-1にした拡大/縮小にあたり」で新しい操作を既習の変換へ着地させる）
- TF-019（「3次元の座標空間では、右手系と左手系の違いがあるからです」を足場に、投影座標系をカメラ座標系との差分（z軸の向きだけ反転）で位置づける）
- TF-020（「z軸の正の向きを手前にとるのが右手系、奥にとるのが左手系でした」／「[幾何学的変換]で順に測り直していきます」）
- TF-021（右手系・左手系はリンクを張るだけでなく「x軸を右、y軸を上に向けたとき…」と記事内で定義を書き直す（この後のz軸反転の判断の根拠になるため））
- TF-024（末尾「モデリング座標系の中だけで定められていた形状の座標は、こうして4つの変換を通り抜け、画面上の位置として決まります」）
- TF-025（「モデリング変換」節はワールド座標系を言い切ってから ### モデリング座標系 / ### モデリング座標からワールド座標へ に、「投影変換」節は ### 投影座標系 / ### 切り取る前のz軸の反転 / ### 正規化デバイス座標系 に落とす）
- TF-026（「シーンがワールド座標系の上に置かれても、それぞれの点が画面のどこに写るかは、まだ求められません」／「視点を原点にとっても、座標系はまだ1つに決まりません」／「正規化デバイス座標系まで来ても、画面のどこに何画素で描かれるかはまだ決まっていません」）
- TF-032（:::Note「最初から左手系でカメラ座標系をとる環境では、この反転は必要ありません」／「環境によってはビューポート変換でy軸の向きが入れ替わることです」／「範囲の取り方は環境によって異なります」）
- TF-037（右手系／左手系の対置、および :::Action でのワールド座標系／カメラ座標系の挙動の並置（「カメラだけが動き」対「場面全体が丸ごと逆向きに動く」））
- TF-038（$$-1 \le x \le 1$$ の例示の直後「x座標が-1より小さければ左へはみ出している、というように、座標値の大小を比べるだけで点が内側にあるかどうかを判定できます」）
- TF-040（:::Info「どのように範囲を切り出し、どんな直方体へ移すのかは[投影の手順]で詳しく扱います」／「詳しくは[階層モデリング]で扱います」／「[投影の計算法]で組み立てます」）
- TF-042（z軸反転行列を出す前に「z座標の符号を変えるだけの操作です。x座標とy座標はそのままなので、z方向の倍率だけを-1にした拡大/縮小にあたり」と各成分の値の理由を先に説明）
- TF-044（視野変換→投影座標系→正規化デバイス座標系→ビューポート変換が、それぞれ直前の段の「まだ決まらないこと」への応答として連鎖している）
- TF-047（第1節で「次の4つの変換を経て、画像上の位置を決定します」と枠組みを列挙し、以降の節で同順に詰める）
- TF-053（:::Note「名前に「デバイス」とありますが、実際に表示するモニターなどの画素で位置を決めるのは、最後のビューポート変換で移る先のデバイス座標系です」）
- TF-057（:::Action「進み具合を100%にしたら、次の変換を選んで…」「上方向の傾きを変えて、カメラが同じ位置・方向にあるのに、写る像の傾きが変わる様子を確かめよう」）
- TF-058（「見てみよう」「確かめよう」「カメラを回したり、距離を変えたりして、2つの座標系での挙動を見比べてみよう」）
- TF-060（「視線はz軸の負の向きになります」「z座標が小さいという並びは」「正規化デバイス座標系では上を正にとるのに対し…下を正にとる」など、反転すると偽になる一語を :Anki にしている）
- TF-061（「ワールド座標系で形状を決めてしまうと、物体を移動させるたびに、物体を構成するすべての点（形状）を再計算することになってしまうからです」／:::Note「この並びを崩すと、狙った場所とは違うところに物体が配置されてしまいます」）
