分析時点: `e2470fa3`
記事タイプ: 概念解説

- TF-047（冒頭節で端点一致性・接ベクトル・凸包性・アフィン不変性を箇条書きで枠づけ→以降のH2で同順に詳述（「いずれも、制御点をどう動かせば曲線がどう変わるかを見通すための手がかりになります」））
- TF-021（導入「[ベジェ曲線]には、曲線上の点が制御点の重み付き平均になるという重要な特徴がありました」／「[n次ベジェ曲線]を表す式は、次のような形をしていました」＋式をTermCardで再掲）
- TF-020（「係数 $$B_{i,n}(t)$$ は、[バーンスタイン基底関数]の式 $$\binom{n}{i} t^i (1-t)^{n-i}$$ で決まります」／「その[グラフ]で見たように、負の値をとることはなく」）
- TF-008（端点一致性を主張で止めず「0のべき乗は、指数が1以上のときは0、指数が0のときだけ1になります。そのため $$i$$ が1以上の係数はすべて0となり消え」と係数の挙動まで下ろす）
- TF-055（「中間の制御点は曲線を引き寄せる目印であって、通過点にはなりません」（制御点をすべて通ると思う誤った心的モデルを先に打ち消す））
- TF-018（「制御点を格子状に結んだ網を制御点網といい、これは曲線のときの制御多角形にあたるもので」（新用語を既出概念へ対応づけて着地））
- TF-053（「[線形補間とベジェ曲線]では、ベジェ曲線が制御多角形の角には届かず、その内側で丸まることについて触れました。凸包という概念によって、この「内側」がどこまでの範囲を指すのかを表すことができます」（制御多角形と凸包を同じ軸で区別））
- TF-027（「この凸包性により、ベジェ曲線が画面の外にあるかどうかや、2本のベジェ曲線が交わる可能性があるかどうかを、曲線上の点を求めずに制御点だけで判定できるようになります」／「このアフィン不変性により…リアルタイムで曲線の形を編集する場面でも、動作が重くなりません」）
- TF-044（「ベジェ曲線上の点は定義式に $$t$$ を代入すれば求められますが、次数が上がるほど、べき乗の計算がかさみます。そこで、同じ点を内分の繰り返しだけで求めるアルゴリズムがド・カステリョのアルゴリズムです」）
- TF-041（「ここまで見てきたベジェ曲線は、パラメータが1つで…曲面に発展させるには、パラメータを追加する必要があります」／「曲線で見た性質は、そのまま曲面にも引き継がれます」（増分だけを説明し、性質の対応で整合を確認））
- TF-048（「実際によく使われるのは、$$u$$ 方向にも $$v$$ 方向にも3次をとる双3次ベジェ曲面です」／「そのため実際には、次数を3程度に抑えたベジェ曲線・曲面をいくつも繋いで使います」）
- TF-032（「一方で、細かく作り込もうとして制御点を増やすと、その分次数が上がって計算が重くなり、1つの制御点を動かしただけで曲線や曲面の全体が変わってしまいます」（手法の適用限界を先回りして示す））
- TF-039（Note「式に現れる $$C^{\prime}(t)$$ は、$$C(t)$$ を $$t$$ について微分したものです。…接ベクトルが表すのはこの変化の割合そのものなので」／「$$P_1 - P_0$$ は、$$P_0$$ から $$P_1$$ へ向かうベクトルです」）
- TF-025（端点一致性・凸包性の本筋を言い切ってから、$$\binom{n}{0}=1$$・$$\binom{n}{n}=1$$ の理由、凸多角形の定義、重み付き平均の割り算をNoteへ落とす）
- TF-022（「制御点に[アフィン変換](/cg/transformation/affine-transformation/)をかけてから」「回転や拡大などの[線形変換](/cg/transformation/basic-transformations/)と、平行移動を組み合わせたもの」（変換の詳細はリンクへ委譲し、線形性の要点だけ埋める））
- TF-057（:::Action「制御点をドラッグして、始点・終点から伸びる接ベクトルが、常に制御多角形の最初の辺・最後の辺と同じ向きであることを確認しよう」／「四隅の高さを動かすと曲面の4辺のベジェ曲線が変化し、内側4点の高さを動かすと内側だけが膨らむことを観察しよう」）
- TF-058（凸包デモ前「制御点がへこんだ位置にあっても、そのへこみが凸包に包まれる様子を見てみよう／また、制御点をドラッグして、ベジェ曲線が凸包からはみ出さないことも確認しよう」）
- TF-059（「内分を繰り返しているだけなので、途中に現れる点も、最後に残る曲線上の1点も、制御点の凸包から出ないことが、作図のデモからそのまま見て取れます」）
- TF-040（末尾「1つの制御点が影響する範囲を局所に限ることで、次数を上げずに多くの制御点を扱えるようにしたものとして、[Bスプライン曲線（曲面）]があります」）
- TF-026（凸包性節冒頭が制御多角形の到達点を受け直し、ベジェ曲面節冒頭「ここまで見てきたベジェ曲線は、パラメータが1つで」で前半の到達点から次を動機づける）
  ※ TF-046：反証で除外：数式へ比喩を添える一般的技法と区別できない
  ※ TF-016：反証で除外：数式解説記事で一般的な提示法と区別できない
  ※ TF-045：反証で除外：コード併載記事一般の技法と区別できない
