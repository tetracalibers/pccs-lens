分析時点: `9d00ec19`
記事タイプ: 概念解説

- TF-001（導入「しかし、そもそもパラメータを動かして曲線を得るには…パラメータを含む数式を知っている必要があります」→ベジェ曲線の必要性を先に立てる）
- TF-061（導入「車体や翼の断面のような形を作りたいとき、その形になる関数を知る手がかりがありません」（既存手法のままでは何ができないかで動機づける））
- TF-044（「そこで、関数を直接考えるのをやめ、いくつかの点の置き方で曲線の形を操れるようにしたのがベジェ曲線です」）
- TF-043（「曲線に入る前に、まずは直線を描くことで、この考え方を探ってみましょう」（2点の線形補間へ単純化してから制御点を足す））
- TF-051（H3見出し「線形補間（1次ベジェ曲線？）」（読者が呼びたくなる名前を疑問符付きで先取りする））
- TF-029（「一般に、ベジェ曲線上の点は、$$n+1$$ 個の制御点に重み $$B_{i,n}(t)$$ をかけて足し合わせたものになります」／「そして、この性質は3次に限らずどの次数でも成り立ちます。つまり、次の式が成り立つのです」（3次の観察→一般式））
- TF-041（「直線を表すには2つの制御点、2次ベジェ曲線では3つの制御点が必要でした。この規則性は高次になっても続き」＋H3「直線の場合」「2次ベジェ曲線の場合」で一般式を $$n=1,2$$ に特殊化して係数を同定）
- TF-013（「フランスの自動車メーカーで車体の設計に用いた、ピエール・ベジェの名が付いています」／「$$B_{i,n}(t)$$ の $$i$$ は、この係数は何個目の制御点 $$P_i$$ につくかを表す数字です」）
- TF-030（Note「`lerp`はlinear interpolationの略、`mix`は、まさにある比率（直線では $$1-t : t$$）で座標値を混合することを表していますね」）
- TF-015（「$$P_0, P_1$$ の間を点で埋めていくことは、…足りない点を計算して滑らかに補うことにあたります。この補う操作を補間、直線の場合は特に線形補間と呼びます」（意味を構築してから命名））
- TF-039（「$$Q_0(t)$$ と $$Q_1(t)$$ に線形補間の式を代入して展開すると、3つの制御点だけで書かれた式になります」／「こうして、$$C(t)$$ は $$t$$ の2次多項式として表されるため」）
- TF-042（「$$\binom{n}{i}$$ は $$n$$ 個から $$i$$ 個を選ぶ組み合わせが何通りあるかを表す数です」＋Note「1回ごとに $$1-t$$ と $$t$$ のどちらかがかかるため…その組み合わせの数が $$\binom{n}{i}$$ にあたります」（二項係数を天下りにしない））
- TF-048（「CGで最もよく使われるのは、制御点を4つ置く3次のベジェ曲線です」（次数一般の話から実務上の焦点へ絞る））
- TF-059（バーンスタイン基底のデモ直後「このグラフから、重みはどの $$t$$ でも正の値をとり、すべての重みを合計すると1になることがわかります」）
- TF-057（:::Action「`t`の値を変えて、4本のグラフの高さが入れ替わる様子を観察しよう／右側の縦帯から、4本のグラフの高さの合計は常に1となることも読み取ろう」）
- TF-058（「好きな制御点をドラッグで動かし、制御多角形と曲線の形を変化させてみよう」など、各デモ前のActionで読者に操作を割り当てる）
- TF-017（「$$B_{i,n}(t)$$ は、各制御点にどれだけの重みをかけるかを決める関数と捉えることもできます」（内分比率という先の説明を重みとして再定義））
- TF-023（線形補間の式 $$(1-t)P_0 + tP_1$$ を、2次ベジェの $$Q_0, Q_1$$ の導出、$$B_{i,1}$$ の同定、重みつき平均のNoteで繰り返し部品として使う）
- TF-020（「同様に、$$n=2$$ の場合（2次ベジェ曲線）の式を思い出すと」／「$$B_{i,n}(t)$$ は…これまでに登場した直線や2次ベジェ曲線を表す式の、$$P_i$$ の前についていた係数部分です」）
- TF-024（「ベジェ曲線の式は重み $$B_{i,n}$$ を制御点にかけ合わせたものですが、この重み $$B_{i,n}$$ の和が1であることから、曲線上の点は制御点の重み付き平均になっているといえます」（積み上げを一語へ圧縮））
- TF-025（重み付き平均という結論を本文で言い切ってから、平均・重み付き平均の一般的な定義をNoteへ落とす）
- TF-040（末尾「具体的にベジェ曲線のどんな振る舞いに繋がるのかは、[ベジェ曲線の数学的性質]で見ていきます」）
