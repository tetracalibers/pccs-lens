分析時点: `69498efc`
記事タイプ: 概念解説

- TF-021（「[陰関数形式]では、座標が満たすべき条件を方程式として与え、それを満たす点の集まりを曲線としました。その左辺を`x`と`y`の2次多項式にしたものが2次曲線で」）
- TF-047（導入で円・楕円・放物線・双曲線の4項目を提示→円錐曲線節で切り口として枠づけ→標準形節とパラメータ形式節で楕円・放物線・双曲線を同順に詰める）
- TF-013（「いずれも同じく円錐面の断面に現れるため、2次曲線は円錐曲線とも呼ばれます」／「母線と呼ばれる1本の直線を、それと1点で交わる軸のまわりに回すことで」）
- TF-054（「底面を持つ立体の円錐とは異なり、円錐面は軸との交点を挟んで上下2つに分かれ、上下どちら側にも限りなく広がっていきます」）
- TF-032（「ただし、$$\cosh$$ は常に1以上の値をとるので、$$x = a\cosh t$$ という式で表せるのは $$x$$ が正となる側の曲線だけです」／双曲線のカード「辿れるのは $$x$$ が正となる側の枝だけ」）
- TF-061（一般式のNote「2次の項につく $$a$$・$$b$$・$$c$$ は、少なくとも1つが0でなければなりません。3つとも0だと式は1次になり、直線になってしまいます」）
- TF-039（「両辺を $$a^2$$ で割ると…となります」「$$\dfrac{x^2}{a^2} = 1 + \dfrac{y^2}{b^2}$$ と書き直すと、右辺は必ず1以上になります」「$$y^2 = 4px$$ に $$y = 2pt$$ を代入すると左辺が $$4p^2t^2$$ になり、両辺を $$4p$$ で割ると」）
- TF-042（放物線のパラメータ形式のNote「$$y$$ の置き方に $$2p$$ 倍を選んでおくのは、$$x$$ の式に分数を出さないためです」）
- TF-008（「$$y$$ の符号を入れ替えても同じように成り立つ式でなければならないので、$$y$$ は2乗の形でしか式に入れません」（式の形の背後にある線対称性まで説明））
- TF-041（「双曲線の標準形は、楕円の標準形の足し算を引き算に変えた形です」／楕円のNote「$$a$$ と $$b$$ が等しいときだけ倍率が揃い、半径 $$a$$ の円となります」（特殊化で整合を確認））
- TF-017（楕円のパラメータ形式「この式は、半径1の円周上の点の $$x$$ と $$y$$ を、それぞれ $$a$$ 倍・$$b$$ 倍したものと読むこともできます」）
- TF-025（本筋（標準形・領域の制限）を言い切ってから、置き換えの意味・2乗の大小と絶対値・三角関数の関係式の意味をNoteへ落とす）
- TF-026（パラメータ形式節冒頭「標準形まで簡単にしても、`x`を決めて`y`を求めるには方程式を解くことになり、曲線上の点を順に並べるのは大変です。そこで、[パラメータ形式]で書き直します」）
- TF-044（一般式（位置・向きで係数が変わる）→標準形（それでも点を並べにくい）→パラメータ形式、と限界を都度言語化して次へ渡す）
- TF-057（:::Action「ピンクの点と水色の点を結ぶ2線が常に同じ長さであることに注目しながら、`p`や`y`を動かしてみよう」／漸近線デモの「次の様子に注目して」＋箇条書き）
- TF-058（「回転角を動かして、1本の母線が動いた跡として円錐面が描かれていく様子を見てみよう」など、各デモを読者の操作として渡す）
- TF-022（「原点を中心に置いた楕円を[平行移動や回転](/cg/transformation/basic-transformations/)すると」（変換の詳細はリンクへ委譲））
  ※ TF-040：末尾に後続記事への委譲・予告がない。記事は双曲線関数デモのActionとFoldableで終わり、締めの一文も次記事への橋渡しも置かれていない（同シリーズの他4記事とは対照的）。標準形節のNoteや漸近線の扱いなど深掘り余地のある論点も、後続へ回す言明なしに閉じている。
  ※ TF-002：反証で除外：題材固有の言い回しの可能性
