分析時点: `53d7c0bb`
記事タイプ: 概念解説

- TF-001（陰関数形式節冒頭「陽関数形式では表せない円などの図形は、別な数式の形で表す必要があります」）
- TF-044（陽関数形式（表せる形が限られる）→陰関数形式（ラスタ化には負荷が高い）→「そこで、座標そのものを補助的な別の変数から計算できる、パラメータ形式がCGではよく使われます」の三段連鎖）
- TF-032（H3見出し「陽関数形式で表せる対象」＋「ただし、陽関数形式で表せる形は限られます」「真横を向いた壁や、下へせり出した形は…ハイトフィールドの枠には収まりません」）
- TF-013（「地形の起伏はこの形で表せるため、こうした曲面はハイトフィールド（高さ場）と呼ばれることがあります」／「この補助的な変数はパラメータまたは媒介変数と呼ばれます」）
- TF-038（$$z = f(x, y)$$ の直後「この形は、平面上のどの点にも高さが1つずつ割り当てられたものと読めます」）
- TF-039（円の陰関数式のNote「両辺を2乗して右辺を移項すると、上に挙げた円の式になります」／交点計算「展開すればxについての2次方程式なので、解の公式で解けます」）
- TF-022（「[ラスタ化](/cg/basics/shape-rasterization/)してレンダリングするには負荷が高い方法です」「[レイトレーシング法](/cg/rendering/ray-tracing-algorithm/)では、このような交点計算が繰り返し行われます」）
- TF-021（冒頭「[さまざまな形状モデル]では、立体を稜線・面・中身のどれで捉えるかによって形状モデルを分類しました」（リンクに任せず前提を一文で書き直す））
- TF-023（同じ円を、陽関数形式で表せない例→陰関数形式 $$x^2+y^2-r^2=0$$→内外判定の $$f$$→交点計算→パラメータ形式 $$r\cos\theta$$ と、節ごとに別角度で再利用）
- TF-027（H3「陰関数形式と内外判定」「陰関数形式と交点計算」で、形式の説明を判定・交点計算という用途へ着地させる）
- TF-041（「3次元空間内の曲線を表したいときは、さらに $$t$$ を引数として $$z$$ 座標を表す関数を用意すればよいだけです」（既出の2次元式に増分だけを足す））
- TF-025（円の陰関数式・多項式の次数を本文で言い切ってから、導出と「項の次数／多項式の次数」の定義をNoteへ落とす）
- TF-026（各H2の第1文が前節の到達点・限界を受ける（「陽関数形式では表せない円などの図形は」「陰関数形式は、与えられた点が形状の上にあるかを判定する用途には向いていますが」））
- TF-057（:::Action「`x`と`y`を動かして、`x`の位置を表す縦線が円・曲面と交わる点を数えてみよう」＋交点の個数を箇条書きで先に指示）
- TF-058（Actionが読者への操作命令（数えてみよう／観察しよう／追ってみよう）で、デモを読者自身の作業として渡す）
- TF-040（末尾「制御点を動かして形を作る[ベジェ曲線]や[Bスプライン曲面]も、パラメータ形式で曲線・曲面を表す手法の一つです」）
  ※ TF-019：反証で除外：パラメータ形式の利点を述べる一般的記述と区別できない
