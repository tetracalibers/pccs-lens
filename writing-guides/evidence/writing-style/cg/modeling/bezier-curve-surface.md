分析時点: `9d00ec19`
記事タイプ: 概念解説

- WS-004（末尾が次記事への予告で終わり、まとめ節がない）
- WS-005（末尾「具体的にベジェ曲線のどんな振る舞いに繋がるのかは、ベジェ曲線の数学的性質で見ていきます」）
- WS-006（線形補間（1次）→2次ベジェ曲線→高次ベジェ曲線の順で、「直線を表すには2つの制御点、2次ベジェ曲線では3つ…この規則性は高次になっても続き」と順序を明示）
- WS-007（リードでパラメータ形式の到達点と限界（形になる関数を知る手がかりがない）を提示してから本題へ）
- WS-010（リード「パラメータ形式は、パラメータを動かせば曲線上の点が次々と得られる、扱いやすい表し方でした。しかし、…」）
- WS-013（リードが前記事の限界提示で閉じ、3節構成（具体例／高次／バーンスタイン基底関数）の予告を置かない）
- WS-015（「## 高次ベジェ曲線」冒頭で1次・2次の制御点数を受け直して $$n$$ 次へ、「## バーンスタイン基底関数」冒頭「各制御点にかかる重み $$B_{i,n}(t)$$ の式には、規則性があることが知られています」）
- WS-018（「### 制御多角形」→「制御点を順に線分で結んだ折れ線を制御多角形といいます」など、全見出しが地の文の一文で始まる）
- WS-022（「## 高次ベジェ曲線」→制御点数の短い導入→TermCard「n次ベジェ曲線の式」、「## バーンスタイン基底関数」→一文の導入→TermCard）
- WS-025（最初の節「ベジェ曲線の具体例」でベジェ曲線→制御点と連鎖定義）
- WS-026（バーンスタイン基底関数節末「バーンスタイン基底関数によって、こうして高次のベジェ曲線の式も求めることができるのです」）
- WS-032（「車体や翼の断面のような形…その形になる関数を知る手がかりがありません」→「そこで、関数を直接考えるのをやめ…したのがベジェ曲線です」）
- WS-034（「ベジェ曲線の具体例」「高次ベジェ曲線」「バーンスタイン基底関数」＋H3「線形補間（1次ベジェ曲線？）」「2次ベジェ曲線」「制御多角形」「制御点と重みつき平均」）
- WS-037（「直線の場合は特に線形補間（linear interpolation）と呼びます」直後のNoteで`lerp`・`mix`という別名を補足）
- WS-042（「この規則性を表した関数をバーンスタイン基底関数といい」／「この重みを表すバーンスタイン基底関数のグラフをそれぞれ描いてみると」）
- WS-048（バーンスタイン基底関数カードの直後「$$\binom{n}{i}$$ は…を表す数です。これに $$t$$ のべき乗と $$1-t$$ のべき乗をかけ合わせた形になっていて、$$i$$ が小さい制御点ほど $$1-t$$ 側の指数が大きくなります」）
- WS-052（「$$P_0, P_1$$ の間の足りない点を計算して滑らかに補うことにあたります。この補う操作を補間、直線の場合は特に線形補間と呼びます」／「こうして、$$C(t)$$ は $$t$$ の2次多項式として表されるため、この曲線は2次ベジェ曲線と呼ばれます」）
- WS-053（「$$B_{i,n}(t)$$ は、制御点間の線分を内分する比率を表す関数であり」→後段「各制御点にどれだけの重みをかけるかを決める関数と捉えることもできます」）
- WS-055（ベジェ曲線・制御点・補間・線形補間・制御多角形・重み・重み付き平均をいずれも初出位置で定義し、`lerp`/`mix`や平均の定義はNoteへ）
- WS-057（$$B_{i,n}(t)$$ の一般形の直後に「### 直線の場合」「### 2次ベジェ曲線の場合」で代表例、基底関数の式の直後に $$n=3$$ の4本を提示）
- WS-059（直線の場合と2次の場合を並べたのち「一般に、ベジェ曲線上の点は、$$n+1$$ 個の制御点に重み $$B_{i,n}(t)$$ をかけて足し合わせたものになります」）
- WS-060（「CGで最もよく使われるのは、制御点を4つ置く3次のベジェ曲線です」）
- WS-061（「n次ベジェ曲線の式」「バーンスタイン基底関数」をTermCard/CardGridで分離）
- WS-063（BernsteinBasisDemoの後「このグラフから、重みはどの $$t$$ でも正の値をとり、すべての重みを合計すると1になることがわかります」）
- WS-064（線形補間の式→内分の読み方→:::Action→LinearInterpolationDemo、制御多角形の定義→性質→:::Action→ControlPolygonDemo）
- WS-065（:::Action「`t`を動かすと、各線分の内分点が動き、`C(t)`が動いた跡が曲線を描くことを観察しよう」）
- WS-068（:::Note（`lerp`・`mix`という呼び名／$$\binom{n}{i}$$ が現れる理由／平均と重み付き平均の定義））
- WS-069（「線形補間（linear interpolation）」「$$n=1$$（直線）」など一句は丸括弧、平均の定義は複数文のためNoteブロック）
- WS-072（パラメータ形式・2次曲線・ベジェ曲線の数学的性質をリンクで委譲）
- WS-073（リード第1文「パラメータ形式は、パラメータを動かせば曲線上の点が次々と得られる、扱いやすい表し方でした」で前提を一文再掲）
- WS-076（$$Q_0(t)$$・$$Q_1(t)$$ の線形補間式→代入→展開して3制御点の式へ、高次節で1次・2次の既出式を再掲してから $$B_{i,n}$$ を取り出す）
