分析時点: `53d7c0bb`
記事タイプ: 概念解説

- WS-004（最終節がパラメータ形式のまま終わり、まとめ節が存在しない）
- WS-005（末尾「CGでは、曲線・曲面の多くがパラメータ形式…／制御点を動かして形を作るベジェ曲線やBスプライン曲面も…」）
- WS-006（陽関数形式→陰関数形式→パラメータ形式の順、および「例：平面上の直線」→「例：平面上の円」→「曲面のパラメータ表示」）
- WS-007（リード「さまざまな形状モデルでは…分類しました。ここからは、その面や線にあたる曲線・曲面の数式表現を見ていきます」）
- WS-010（リード（前記事へのリンク＋前記事の到達点の再掲＋本記事の対象の予告））
- WS-013（リードが主題（曲線・曲面の数式表現）の設置だけで閉じ、3形式の構成予告を置かない）
- WS-015（「陰関数形式」冒頭「陽関数形式では表せない円などの図形は、別な数式の形で表す必要があります」／「パラメータ形式」冒頭「陰関数形式は…負荷が高い方法です。そこで、」）
- WS-018（全見出し（陽関数形式／陰関数形式と内外判定／例：平面上の直線のパラメータ表示 など）が地の文の一文で始まる）
- WS-020（「曲面でも同じ問題が起こります」／「曲面でも同じで、視線を表す式と曲面の式を連立させれば」）
- WS-032（「陽関数形式で表せる対象」で限界を示した直後に陰関数形式を導入、陰関数形式の計算負荷を示して「そこで、…パラメータ形式がCGではよく使われます」）
- WS-034（見出しが「陽関数形式」「陰関数形式と内外判定」「陰関数形式と交点計算」「代数曲線と代数曲面」など概念名詞で1見出し1概念）
- WS-036（「陽関数形式は、一方の座標を、もう一方の座標の関数として与える形式です」直後に $$y=f(x)$$ へ展開）
- WS-037（「こうした曲面はハイトフィールド（高さ場）と呼ばれることがあります」／「この補助的な変数はパラメータまたは媒介変数と呼ばれます」）
- WS-039（「次数が2の代数曲線は2次曲線と呼ばれ…これらはいずれも円錐を平面で切った断面として現れるため、円錐曲線とも呼ばれます」）
- WS-041（「$$x$$ を決めれば $$y$$ が計算だけで求められ、その座標 $$(x, y)$$ の点を並べることで曲線を描きます」（連用中止で横に伸ばし従属節を入れ子にしない））
- WS-042（「この形は、平面上のどの点にも高さが1つずつ割り当てられたものと読めます」／「この $$f$$ は、原点からの距離の2乗と、半径の2乗との差になっています」）
- WS-053（陰関数形式の定義後に曲線・曲面それぞれで言い直し（陰関数曲線／陰関数曲面）、さらに円の式で具体化）
- WS-055（陽関数形式・ハイトフィールド・陰関数曲線・多項式・パラメータをいずれも初出位置でその場定義し、次数の定義はNoteへ回す）
- WS-057（$$f(x,y)=0$$ の一般形の直後に「原点を中心とする半径 $$r$$ の円は、陰関数形式では次のように書けます」）
- WS-058（「たとえば円のような、1つの $$x$$ に2つの $$y$$ が対応する曲線は表せません」など、例示ブロックを使わず地の文に埋める）
- WS-059（直線・円・曲面のパラメータ表示を並べたのち「CGでは、曲線・曲面の多くがパラメータ形式…で表されます」へ着地）
- WS-064（陽関数形式の定義→表せる対象（限界）→:::Action→ExplicitFormLimitsDemo、直線の式→読み方→:::Action→LineParameterFormDemo）
- WS-065（:::Action「`x`と`y`を動かして、`x`の位置を表す縦線が円・曲面と交わる点を数えてみよう」など、説明とデモの間に着眼点ブロック）
- WS-068（:::Note（三平方の定理からの円の式の導出／項の次数・多項式の次数））
- WS-069（「ハイトフィールド（高さ場）」「その座標 $$(x, y)$$ の点」など一句の補足は丸括弧、導出はNoteブロック）
- WS-071（「陽関数形式」節の後に「### 陽関数形式で表せる対象」で制約を追補、「陰関数形式」の後に内外判定・交点計算をH3で追補）
- WS-072（ラスタ化・レイトレーシング法・2次曲線・パラメトリック曲線（曲面）などをリンクで委譲）
- WS-073（リード第1文で前記事の到達点「立体を稜線・面・中身のどれで捉えるかによって形状モデルを分類しました」を一文で再掲）
- WS-076（交点計算（円の式へ直線を代入→2次方程式→解の個数の解釈）、Note内の $$\sqrt{x^2+y^2}=r$$ →2乗→移項の操作説明と表示式の交互）
  ※ WS-063：ExplicitFormLimits・LineParameterForm・CircleParameterForm・ParametricSurfaceMappingのいずれもデモ直後は:::Foldableの実装コードで、読み取り結論の一文はデモ後に置かれない。読み取るべき内容は直前の:::Action（「交わる点を数えてみよう」「点の集まりが円周を描いていく様子を観察しよう」）に前置きされている
