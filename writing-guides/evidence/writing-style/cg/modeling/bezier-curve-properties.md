分析時点: `e2470fa3`
記事タイプ: 概念解説

- WS-001（リード「これらの性質は、ベジェ曲線を重み付き平均という視点で捉えることで解釈できます」以降、端点一致性・凸包性・アフィン不変性すべてを重み付き平均の一軸で説明）
- WS-004（末尾がBスプラインへの言及で終わり、まとめ節がない）
- WS-005（末尾「1つの制御点が影響する範囲を局所に限ることで…Bスプライン曲線（曲面）があります」）
- WS-011（リード「ここから、ベジェ曲線の有用性を支えるさまざまな性質が導かれます」で複数項目の到来を予告）
- WS-016（ド・カステリョの手順1〜3が前工程の産物（$$Q$$→$$R$$→$$C$$）を受けて順接でつながる）
- WS-018（「## ベジェ曲線の凸包性」→「いくつかの点を囲む最小の凸多角形…を凸包といい」など、全見出しが地の文で始まりCardGrid・箇条書きを直置きしない）
- WS-019（「### 端点での接ベクトル」→「ベジェ曲線の端点では、曲線が進む向きも制御点から決まります」）
- WS-020（「$$t$$ が1の場合も同様に、曲線の終点は $$P_n$$ に一致します」／「ベジェ曲面でも方向ごとに同じ関係が成り立つので」／「曲線で見た性質は、そのまま曲面にも引き継がれます」）
- WS-026（各節末の凝縮文「曲線の始点と終点を狙った位置に合わせたいときは、最初と最後の制御点をそこへ置けばよいのです」／「この凸包性により…制御点だけで判定できるようになります」／「リアルタイムで曲線の形を編集する場面でも、動作が重くなりません」）
- WS-028（ベジェ曲面節の散文説明の後に「曲線で見た性質は、そのまま曲面にも引き継がれます」＋3項の箇条書き）
- WS-029（「端点一致性：…一致する」「凸包性：…はみ出さない」「アフィン不変性：…一致する」を同一文型で反復）
- WS-030（「## ベジェ曲線の性質」で4性質を箇条書きで予告し、以降の節で個別詳述）
- WS-031（予告した端点一致性→接ベクトル→凸包性→アフィン不変性の順が、そのまま「端点一致性」節・その中のH3「端点での接ベクトル」・「凸包性」節・「アフィン不変性」節の順序に対応）
- WS-032（「定義式に $$t$$ を代入すれば求められますが、次数が上がるほど、べき乗の計算がかさみます。そこで、…ド・カステリョのアルゴリズムです」）
- WS-034（「ベジェ曲線の性質」「ベジェ曲線の端点一致性」「ベジェ曲線の凸包性」「ベジェ曲線のアフィン不変性」「ド・カステリョのアルゴリズム」「ベジェ曲面」）
- WS-036（「いくつかの点を囲む最小の凸多角形（空間中であれば凸多面体）を凸包といい、ベジェ曲線は制御点が作る凸包の中に収まります」の定義開始→重み付き平均による機構へ展開）
- WS-041（「隣り合う制御点を $$t:(1-t)$$ の比で内分（線形補間）して新しい点を作り、できた点どうしを線形補間することを繰り返すことで、最終的に曲線上の1点を突き止められます」）
- WS-042（「ここで現れた最終的な右辺は、ベジェ曲線 $$C(t)$$ をそのまま $$d$$ だけ平行移動した式になっています」／「ここで現れる0のべき乗では」）
- WS-045（TermCard「n次ベジェ曲線の式」内「$$t$$ をパラメータとし、曲線上の点 $$C(t)$$ は、全制御点 $$P_0,\ldots,P_n$$ の重み付き平均となる」（常体・体言止め））
- WS-048（n次ベジェ曲線の式カード直後「$$t$$ は0から1の範囲で動くので、曲線の始点は $$t=0$$ の場合の $$C(0)$$、終点は…」で端点一致性へ結びつける）
- WS-051（「この手順を擬似コードで表すと、次のようになります」→擬似コード→「`n`はベジェ曲線の次数で、最初の列`p[0]`に入れる制御点は…`r`は何回目の内分でできた点列かを、`i`はその列の中で何番目の点かを表します」）
- WS-052（「曲線にアフィン変換を適用した結果と、アフィン変換を適用した制御点から作られる曲線が一致する性質を、アフィン不変性といいます」（先に振る舞いを述べてから命名））
- WS-055（接ベクトル・凸包・凸包性・重心・制御点網を初出位置で定義し、凸多角形や微分の意味はNoteへ）
- WS-056（リードの直後に4性質を「用語：説明」形式の箇条書きで列挙、ベジェ曲面節でも3性質を箇条書き）
- WS-057（内分の繰り返しという一般手順の直後に「たとえば、4つの制御点 $$P_0,P_1,P_2,P_3$$ で決まる3次ベジェ曲線であれば、次のような手順です」）
- WS-060（「実際によく使われるのは、$$u$$ 方向にも $$v$$ 方向にも3次をとる双3次ベジェ曲面です。制御点は縦横4点ずつ、合わせて16個になります」／「実際には、次数を3程度に抑えた…」）
- WS-061（「n次ベジェ曲線の式」「ベジェ曲面の式」をTermCard/CardGridで分離、$$B_{i,n}(t)\geq0,\ \sum B_{i,n}(t)=1$$ をブロック数式で独立配置）
- WS-064（接ベクトルの式と向きの説明→:::Action→EndpointTangentDemo、凸包の定義と範囲の説明→:::Action→ConvexHullDemo）
- WS-065（:::Action「制御点をドラッグして、始点・終点から伸びる接ベクトルが、常に制御多角形の最初の辺・最後の辺と同じ向きであることを確認しよう」）
- WS-067（端点一致性を示した直後に例外側の注意「これらの端点の間にある制御点…では、どの $$t$$ でも重みが1にはなりません。…中間の制御点は曲線を引き寄せる目印であって、通過点にはなりません」）
- WS-068（:::Note（$$\binom{n}{0}$$・$$\binom{n}{n}$$ が1になる理由／$$C^{\prime}(t)$$ が微分であること／凸多角形の意味／重み付き平均は本来割る／制御点数の数え方））
- WS-069（「（空間中であれば凸多面体）」「重み（係数）」「凸な範囲（へこみのない範囲）」「辺（外部との境界線）」は丸括弧、微分や組み合わせの説明はNoteブロック）
- WS-072（ベジェ曲線・n次ベジェ曲線・バーンスタイン基底関数・制御多角形・重み付き平均・アフィン変換・線形変換を既出ページへのリンクで委譲）
- WS-073（「n次ベジェ曲線を表す式は、次のような形をしていました」／「線形補間とベジェ曲線では、ベジェ曲線が制御多角形の角には届かず、その内側で丸まることについて触れました」）
- WS-076（$$B_{i,n}(0)$$・$$B_{i,n}(1)$$ の代入計算、平行移動の総和の分解→重みの和が1→$$C(t)+d$$、冒頭でn次ベジェ曲線の式を再掲）
  ※ WS-050：ド・カステリョの手順は番号付きリスト1〜3で示されるが、直後に1本の合成式でまとめる段落はなく、:::Action→デモ→擬似コードへ進む（合成式ではなく擬似コードと変数の解説で受けている）
