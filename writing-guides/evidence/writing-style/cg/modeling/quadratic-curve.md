分析時点: `69498efc`
記事タイプ: 概念解説

- WS-001（リードの「係数を変えるだけで同じ式に収まり、しかも同じ図形から切り出せる」という軸が一般式・円錐曲線・標準形・パラメータ形式の全節を貫く）
- WS-003（先頭節「2次曲線の一般式」で総論（一般式と6係数）を先に置く）
- WS-004（双曲線のパラメータ形式のデモで記事が終わり、まとめ節がない）
- WS-012（見出しなしリードの2つの理由（同じ式に収まる／同じ図形から切り出せる）が、そのまま「2次曲線の一般式」「円錐曲線」の順で節になる）
- WS-015（「パラメータ形式」冒頭「標準形まで簡単にしても、`x`を決めて`y`を求めるには方程式を解くことになり…そこで、パラメータ形式で書き直します」）
- WS-018（「### 楕円」→「楕円の標準形の式は、円の式を軸ごとに引き伸ばしたものとして導かれます」など、全見出しが地の文で始まりCardGridを直置きしない）
- WS-019（「### 双曲線」→「双曲線の標準形は、楕円の標準形の足し算を引き算に変えた形です」（見出し語を一般命題へ開き直す））
- WS-020（「また、円・楕円・放物線・双曲線は、いずれも円錐面を平面で切った切り口として現れます」／「また、このパラメータ形式は、標準形の式と次のように繋がります」）
- WS-021（「三角関数の関係式」＋「楕円の標準形」、「双曲線関数の関係式」＋「双曲線の標準形」をCardGridで横並び）
- WS-022（「### 楕円」（パラメータ形式）→「楕円は、角度 $$\theta$$ をパラメータにとります」→TermCard、「### 放物線」も同型）
- WS-028（円錐面の散文説明の後に、切り方3種（ゆるやか／平行／急）を箇条書きで一覧化）
- WS-029（「母線より〜で切ると、…になる」を3項で同一文型反復）
- WS-031（円錐曲線節で列挙した楕円→放物線→双曲線の順序が、標準形節・パラメータ形式節のH3小節の順序と一致）
- WS-032（一般式では位置・向きで係数が変わる曖昧さを示してから標準形を導入、標準形の解きにくさを示してからパラメータ形式を導入）
- WS-034（「2次曲線の一般式」「円錐曲線」「標準形」「パラメータ形式」＋H3「楕円／放物線／双曲線」）
- WS-037（「いずれも同じく円錐面の断面に現れるため、2次曲線は円錐曲線とも呼ばれます」／「母線と呼ばれる1本の直線」）
- WS-041（「円錐面は、母線と呼ばれる1本の直線を、それと1点で交わる軸のまわりに回すことでみられる図形です」など、修飾を横に並べる長文）
- WS-042（「このような平行移動や回転を取り消し、中心を原点に合わせて対称軸を座標軸に重ねると」／「この2つへの距離が等しいという条件を式にすると」）
- WS-043（「$$t$$ の符号を入れ替えると、$$x$$ は変わらないまま $$y$$ だけが符号を変えます」）
- WS-045（TermCard「楕円の標準形」内「$$a$$ と $$b$$ は、中心から曲線までの $$x$$ 軸方向・$$y$$ 軸方向の距離。等しいときが半径 $$a$$ の円」（常体・体言止め））
- WS-048（放物線のパラメータ形式カード直後「$$y$$ は $$t$$ に比例して増える一方、$$x$$ は $$t$$ の2乗で増えるので…」で1点だけ取り出す）
- WS-056（円錐面の定義の後に切り口3分類を箇条書き）
- WS-058（「たとえば、原点を中心に置いた楕円を平行移動や回転すると、係数が0から変化し…」を地の文で処理（Exampleブロックなし））
- WS-059（楕円・放物線・双曲線の3ケースを並べた末に「いずれも同じく円錐面の断面に現れるため、2次曲線は円錐曲線とも呼ばれます」へ着地）
- WS-061（楕円・放物線・双曲線の標準形とパラメータ形式をTermCard/CardGridで本文から分離）
- WS-064（円錐面の定義→:::Action→GeneratrixRevolutionDemo、焦点・準線の定義→:::Action→FocusDirectrixDemo）
- WS-065（:::Action「ピンクの点と水色の点を結ぶ2線が常に同じ長さであることに注目しながら、`p`や`y`を動かしてみよう」）
- WS-066（双曲線のパラメータ形式の節末「ただし、$$\cosh$$ は常に1以上の値をとるので、…表せるのは $$x$$ が正となる側の曲線だけです」）
- WS-067（一般式の直後のNote「2次の項につく $$a$$・$$b$$・$$c$$ は、少なくとも1つが0でなければなりません」）
- WS-068（:::Note（置き換えによる引き伸ばしの理屈／点と準線の距離／2乗の大小と絶対値／$$2p$$ 倍を選ぶ理由））
- WS-069（「（軸に垂直な切り方をすると円になる）」を箇条書き内の丸括弧で、導出はNoteブロックへ）
- WS-072（陰関数形式・平行移動や回転・パラメータ形式を既出ページへのリンクで委譲）
- WS-073（「陰関数形式では、座標が満たすべき条件を方程式として与え、それを満たす点の集まりを曲線としました」と節第1文で前提を圧縮再掲）
- WS-076（楕円の標準形の導出（両辺を $$a^2$$ で割る→分母を $$b$$ に取り替える）、放物線の距離条件→2乗→整理、パラメータ形式で標準形の式を再掲して代入）
  ※ WS-005：記事末尾は双曲線関数のデモと:::Foldableの実装コードで終わり、次記事（ベジェ曲線）への橋渡し文が置かれていない。同シリーズの他3本は末尾に橋渡し文を持つ
  ※ WS-013：リード第2文が「係数を変えるだけで同じ式に収まり、しかも同じ図形から切り出せるからです」と、記事の2つの節に対応する見通しを提示している（主題語の設置だけで閉じていない）
  ※ WS-063：GeneratrixRevolution・ConicSection・FocusDirectrix・HyperbolaAsymptotesの各デモは直後が:::Foldableの実装コードで、読み取り結論の一文が続かない。読み取り内容は直前の:::Action側に前置きされている
