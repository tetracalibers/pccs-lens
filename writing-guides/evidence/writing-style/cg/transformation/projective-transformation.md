分析時点: `ade9282e`
記事タイプ: 概念解説

- WS-004（「まとめ」節を置かず、最終小節 `### 発展：平行線と無限遠点` の末尾に見出しなしの2文「平行性を保たない射影変換では、「平行な線がどこかで交わる」という、一見不思議な現象が起こります。」「そして、この現象は現実世界での遠くのものの見え方と一致するため、…射影変換が重要となるのです。」を置き、意義づけへ引き上げて閉じる。要点の再掲リストはない）
  ※ WS-004：次のトピック（透視投影）への言及は締め段落ではなく本文中の `:::Note`（「射影変換は、…:Anki[透視投影]の基礎となります。」）に寄せている
- WS-010（リード「[アフィン変換](/cg/transformation/affine-transformation/)は、同次座標行列の最下行が`(0 0 1)`に固定された変換でした。」がリンク付き過去形で先行記事の到達点を要約し、次文「この固定を外し、最下行を一般の値まで広げると、遠近感を表せる:Anki[射影変換]が得られます。」で本記事が付け加える点を提示する）
  ※ WS-010：橋渡しの締めが予告文でも問いでもなく、主題語（射影変換）を設置する文のまま最初の `##` へ入る
- WS-013（リードは2文で主題語の設置だけで閉じ、構成予告・目的説明・学習目標を置かない）
  ※ WS-013：CG系の記事だが、リードに射程・注目点の宣言（「ここでは、…見ていきます」型）を置かない
- WS-015（`## 射影変換` 第1文「同次座標の一般形を使うことで、アフィン変換をさらに拡張した:Anki[射影変換]を定義できます。」が直前の `##` の見出し語を字面で引き、それを材料に節の主題を立てる。概念が積み上がる記事のため受け直しは工程完了型ではなく「〜を使うことで」）
  ※ WS-015：`### 通常の座標への戻し方` 冒頭の受け直し（「同次座標の一般形で解説した:Anki[正規化]を使って」）は第1文ではなく第2文にあり、参照先も直前の小節ではなく2つ前の `##` である
- WS-018（`## 同次座標の一般形`「アフィン変換までの変換では、…同次座標`(x, y, 1)`で表してきました。」／`### 同次座標の同値関係`「定数倍だけ異なる同次座標どうしは、同じ点を指します。」／`## 射影変換`／`### 射影変換で保たれる性質` と、すべての見出し直下が地の文。数式ブロック・`:::Action`・デモはいずれも地の文の後ろ）
- WS-032（`### 通常の座標への戻し方` 冒頭「射影変換では、変換後の`w`が`1`以外の値をとるため、変換後の同次座標`(x', y', w')`をそのまま通常の座標として扱うことはできません。」で扱えないことを名指しし、続く文「そこで、…:Anki[正規化]を使って、…通常の座標へ戻します。」で操作を導入する）
  ※ WS-032：`## 射影変換` は前節の到達点（同次座標の一般形）がそのまま道具になるため不足の指摘を置かず、「〜を定義できます」の宣言から入る
- WS-034（`##` は「同次座標の一般形」「射影変換」の概念名詞2つ。`###` は各 `##` に2個・3個で、「同次座標の同値関係」「通常の座標への戻し方」「射影変換で保たれる性質」と細目を名詞句で切り出す）
  ※ WS-034：`### 定数倍しても同じ点を表す` は名詞句ではなく述語の形で、性質の主張をそのまま見出しにしている
- WS-040（段落は1〜3文。「一方、`x`と`y`をそのままにして`w`だけを変えると、…」の段落が3文、`### 射影変換で保たれる性質` 冒頭は主張→限定→帰結の3文を改行せず1行に連結する。4文以上の段落は全編にない）
- WS-048（$$w^{\prime} = p x + q y + r$$ の直後に「ここで重要なのは、割る値である…が、点の位置`x, y`によって変わるという点です。」と注目点を1つだけ取り出し、遠近感の説明へ結びつける。定数倍の式の直後も「分子と分母から`k`が約分されて消えるため、…」と読み取りを1つに絞る）
- WS-049（アフィン変換と射影変換を「アフィン変換では最下行の`(p q r)`が`(0 0 1)`だったので、…」「一方、射影変換では、`p`や`q`が`0`でないため、…」と同じ文型・同じ粒度で並置し、差分（`w'`が座標に応じて変化する）を名指す。`### 発展：平行線と無限遠点` でも「アフィン変換では最下行が`(0 0 1)`なので、…」「ところが射影変換では、…」で同型に並置する）
  ※ WS-049：対比を見出しやカードのレベルに立てず、連続する2段落の地の文で処理している
- WS-052（「定数倍だけ異なる同次座標どうしは、同じ点を指します。つまり…。このような関係を:Anki[同値]と呼び」／「この「定数倍してよい」という自由度のもとで、…代表点として選ぶ操作を:Anki[正規化]といいます。」と、成り立ち・操作を述べてから名づける）
  ※ WS-052：`## 射影変換` の主題語だけはリードで名前を予告済みのため、節の第1文で先出しされる
  ※ WS-052：命名の位置は「第1段落の末尾」に固定されていない。:Anki[正規化] は `### 同次座標の同値関係` の3段落目で名づけられる
- WS-065（`:::Action` を3つ（「デモで:Mark[k（定数倍）]を動かすと、点`k(x, y, w)`が直線の上を動くだけ」「…`w = 1`の平面との交点`(x/w, y/w, 1)`は動かない」「デモで:Mark[w]を動かすと、直線ごと向きが変わり、…観察しよう」）置き、いずれも直前の地の文で説明し終えた事実の観察指示にとどめる。実装コードはデモの後ろの `:::Foldable` に置く）
  ※ WS-065：デモ本体は節の後方に1つだけ置かれ、着眼点は説明の途中に複数挟まれる。デモまで距離があるため冒頭に誘導の `:::Action`（「デモまで下にスクロールしてね！」）を足している
- WS-068（`:::Note` を2つ、いずれも1〜2文で本筋の外へ切り出す。射影変換の位置づけ（透視投影の基礎になる）を該当説明の直後に、:Anki[消点] という名づけを無限遠点の説明の直後に置く）
- WS-072（前提記事へのリンクは用語の初出のみで、同じ文の中に必要な要点を一句添える（「[アフィン変換](…)は、同次座標行列の最下行が`(0 0 1)`に固定された変換でした」「[同次座標](…)では、`w`の値は`1`に限らず」）。アフィン変換・同次座標が後の節で再登場する箇所ではリンクを張り直さない）
- WS-076（操作説明→表示式を交互に積む。「これを通常の座標に戻すと、次のようになります」→約分の式→読み取り／「この掛け算を、最下行に注目して展開してみます」→$$w^{\prime} = p x + q y + r$$→注目点／「…正規化を使って、…`w'`で割ることで通常の座標へ戻します」→矢印の式→「こうして得られた`(x'/w', y'/w')`が、射影変換後の実際の座標となります。」。表示式が2つ連続する箇所はない）
