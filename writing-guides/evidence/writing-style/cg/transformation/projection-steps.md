分析時点: `de7b964b`
記事タイプ: 概念解説

- WS-004（「まとめ」節を置かず、最終小節 `### 正規化ビューボリュームの透視投影` の末尾（`:::Note` の後）に見出しなしの2文「透視投影と平行投影によって処理が分かれるのは、:Anki[ビューボリュームの正規化]までです。」「:Anki[透視投影]では奥にあるものを:Anki[縮ませる]処理が必要ですが、…どちらの投影でも同じになります。」を置き、記事全体で処理が分岐する範囲を整理して閉じる）
- WS-010（リード「空間の点が投影面のどこへ写るかが分かっても、それだけでは`1`枚の画像にはなりません。投影面のどの範囲を画像とし、空間のどこまでを写す対象にするのかも、合わせて決める必要があります。」が、先行内容の到達点を受けて足りないもの＝本記事の主題を置く。予告した2点が第1節「ウィンドウ」・第2節「クリッピング面」の順にそのまま対応する）
  ※ WS-010：前記事への内部リンクを張らず、内容だけで受けている
- WS-013（リードは2文で、決めるべき事柄の設置だけで閉じ、構成予告・目的説明・学習目標を置かない）
  ※ WS-013：CG系の記事だが、リードに射程・注目点の宣言（「ここでは、…見ていきます」型）を置かない
- WS-015（`## クリッピング面` 第1文「:Anki[ウィンドウ]は、投影面の写った像のうち、どこを切り取るかを決めるものでした。」が直前の `##` の見出し語を字面で引いて役割を過去形で要約し、次文「次に決めるべきは、投影面に写す対象の絞り込みです。」で新テーマへ渡す。`## ビューボリューム` 第1文も「ウィンドウを通る投射線が作る立体を、この`2`枚のクリッピング面で切り取ったものが…」と前2節の見出し語を材料に引く）
  ※ WS-015：`###` 小節の頭では前節参照を置かず、その小節の主題から入る
- WS-018（`## ウィンドウ`「空間の点が:Anki[投影面]上のどこへ写るかが決まっても、…」／`## クリッピング面`／`## ビューボリューム`／`## 正規化ビューボリューム`／各 `###` と、すべての見出し直下が地の文。CardGrid・`:::Action`・デモ・数式はいずれも地の文の後ろ）
- WS-023（同格の2項目の展開先を分量で分ける。透視／平行のウィンドウは注記・デモを伴うため `### 透視投影のウィンドウと画角`・`### 平行投影のウィンドウと画角` に割り、各1〜2文で言い切れるビューボリュームの2形は予告1文＋CardGrid で処理する）
- WS-030（`## ビューボリューム` で「ビューボリュームの形は、投影の種類によって変わります。」と軸を1文で予告した直後に、透視投影・平行投影の2枚の TermCard を置く）
- WS-032（`## ウィンドウ` 冒頭「…画像として切り取る範囲は決まっていません。／そこで、投影面の上に長方形の枠を置き、…」、`## クリッピング面`「そもそも投影面にどれを映し、…その制約はまだ決めていません。…／そこで、ウィンドウの前後に面を用意し、…」、`## 正規化ビューボリューム`「…毎回変わってしまうため、その度にクリッピングの再計算が必要です。…／そこで、クリッピングに入る前に、…:Anki[直方体]へ変換します。」と、3つの `##` すべてで欠けているものを名指してから「そこで、」で操作を導入する）
  ※ WS-032：`## クリッピング面`・`## 正規化ビューボリューム` では「そこで、」の文が節冒頭の3段落目にあり、不足の指摘と解決の導入が節の第1文には収まっていない
- WS-034（`##` は「ウィンドウ」「クリッピング面」「ビューボリューム」「正規化ビューボリューム」の概念名詞4つ。`###` は各 `##` に0〜2個で、いずれも名詞句（「透視投影のウィンドウと画角」「クリッピング」「透視投影の正規化」など））
  ※ WS-034：`## クリッピング面` は `###` を持たず、本文3段落＋箇条書き＋`:::Note` で閉じている
- WS-040（段落は1〜3文。`## クリッピング面` の各段落は2文、`## 正規化ビューボリューム` では1文だけの段落を3つ連続させて工程を刻む）
  ※ WS-040：`### 正規化ビューボリュームの透視投影` の第2段落は「しかし、画像は:Anki[平面]です。」から「どの点も、…像になります。」まで4文が同一段落に入る
- WS-045（TermCard の中身は「投射線が投影中心から:Anki[放射状]に広がるため、…:Anki[四角錐台]になる」「この立体を:Anki[視錐台]という」のように常体・体言止めで、各カード2文以内）
- WS-052（「この枠を:Anki[ウィンドウ]といいます」「…手前にある面を:Anki[前方クリッピング面]、…奥にある面を:Anki[後方クリッピング面]といいます」「…切り取ったものが:Anki[ビューボリューム]です」「この変換後のビューボリュームを:Anki[正規化ビューボリューム]といいます」と、いずれも成り立ち・操作を述べてから名づける。対になる2語は同一文で同時に名づける）
  ※ WS-052：命名の位置は「第1段落の末尾」に固定されていない。前方／後方クリッピング面と正規化ビューボリュームはいずれも節冒頭から3段落目で名づけられる
- WS-065（3つのデモそれぞれの直前に `:::Action` を1つ置き、`:Mark[操作対象]`＋「観察してみよう／確認しよう」に収める。実装コードはデモの後ろの `:::Foldable` に置く）
  ※ WS-065：ビューボリュームのデモ前の `:::Action` は「次の点に着目して」＋箇条書き3項目の形で、うち投影面までの距離・クリッピング面を動かす効果は直前の本文に明示がなく、ブロック内で新しい示唆を持ち込んでいる
- WS-068（`:::Note` を3つ、いずれも1〜2文で本筋の外へ切り出す。レンズの区別との対応をウィンドウの節末に、視点より後ろの点が像を持つ話をクリッピング面の説明直後に、奥行きを落とす処理を最後に行う理由を記事末に置く）
  ※ WS-068：別ページへの委譲（「クリッピングの計算方法は、…ページで詳しく学びます。」）だけは `:::Note` ではなく `:::Info` に入れている
- WS-072（前提記事へのリンクは用語の初出のみで、同じ文の中に必要な要点を一句添える（「[投影中心](…)は光が集まる点であり」「[平行投影](…)では、投影中心が無限遠にあるため」）。投影中心が後の節で再登場する箇所ではリンクを張り直さない）
  ※ WS-072：`:::Note` 内で広角レンズ・望遠レンズの2語から同一ページ（ピンホールカメラ）へリンクしており、1記事1リンクではなく語ごとに張っている
- WS-073（リード「空間の点が投影面のどこへ写るかが分かっても、それだけでは`1`枚の画像にはなりません」を、第1節冒頭で「空間の点が:Anki[投影面]上のどこへ写るかが決まっても、画像として切り取る範囲は決まっていません」とほぼ同内容で言い直して再掲する。リードと第1節冒頭の重複を冗長として削っていない）
