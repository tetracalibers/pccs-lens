分析時点: `1557e364`
記事タイプ: 概念解説

- WS-004（最終節がビューポート変換で、まとめ節を設けず一文の凝縮で閉じる）
- WS-013（リードが「物体に与えられた座標が画面上の位置として決まるまでには、さまざまな座標系を経由します。」の一文のみ）
- WS-015（「投影変換」節冒頭「視点を基準にした座標が得られたら、写す範囲を切り取る段階に入ります。」／「ビューポート変換」節冒頭「正規化デバイス座標系まで来ても、画面のどこに何画素で描かれるかはまだ決まっていません。」）
- WS-016（「シーンがワールド座標系の上に置かれても…まだ求められません」「視点を基準にした座標が得られたら」と、前工程の完了を参照して順接でつなぐ）
- WS-018（「座標系間の変換」「モデリング変換」「### モデリング座標系」など、H2・H3すべて地の文一文で開始）
- WS-028（「### カメラの置き方」で位置・向き・上方向を散文で説明した後「そのため、カメラの置き方を決めるには…」＋3項の箇条書きで一覧化）
- WS-030（第1節で4つの変換を番号付きリストで予告し、以降のH2節で1つずつ詳述）
- WS-031（番号付きリスト「モデリング変換／視野変換／投影変換／ビューポート変換」の項目名と順序が、後続H2見出しにそのまま対応）
- WS-032（「`1`つの座標系ですべてを引き受けようとすると…扱いにくくなります。そこで、目的に合った座標系をいくつも用意し…」／「視点を原点にとっても、座標系はまだ`1`つに決まりません。」から投影座標系を導入）
- WS-034（H3で「モデリング座標系」「モデリング座標からワールド座標へ」「投影座標系」「切り取る前のz軸の反転」等へ細分）
- WS-037（「モデリング座標系といい、ローカル座標系とも呼ばれます」「カメラ座標系といい、視点を基準にとることから視点座標系とも呼ばれます」「正規化とついた名称になっています」）
- WS-042（「この座標系を投影座標系といいます」「この`3`つの規定が、そのままカメラ座標系の骨組みになります」など、前文の結論を指示句で受け直して次文を起こす）
- WS-044（「物体の形状は、それがシーンのどこに置かれるかとは切り離して決められます。ワールド座標系で形状を決めてしまうと…再計算することになってしまうからです。」）
- WS-046（「### 切り取る前のz軸の反転」＝言葉での説明（z座標の符号を変えるだけ）→同次座標の4行4列行列の式を到達点に置く）
- WS-048（行列の式の直後「そのため、この行列は投影変換の行列とあらかじめ掛け合わせて`1`本にまとめておけます。」／NDCの範囲式の直後「このように軸ごとの値の範囲で表現すると…判定できます。」）
- WS-056（「次の`4`つの変換を経て…」＋番号付きリスト、「位置・向き・上方向の`3`つの定義が必要です」＋箇条書き）
- WS-059（「形を作る、場面に並べる、視点から見る、画面へ描き出すといった各段階」＝具体4つ→上位語「各段階」）
- WS-060（正規化デバイス座標系の節に $-1 \le x \le 1,\ -1 \le y \le 1,\ 0 \le z \le 1$ という具体的な範囲を置く）
- WS-061（z軸反転の行列式とNDCの範囲式を、本文から切り出した独立の数式ブロックに置く）
- WS-065（各デモ直前の:::Action「上方向の傾きを変えて、カメラが同じ位置・方向にあるのに、写る像の傾きが変わる様子を確かめよう」）
- WS-067（投影座標系の規則の直後にNote「最初から左手系でカメラ座標系をとる環境では、この反転は必要ありません。」）
- WS-068（変換の合成順序、逆行列、「デバイス」の名称、隠面消去などをNote/Infoに切り出し、関連箇所の直後に配置）
- WS-069（「座標系（座標の測り方）」「カメラ（視点）」「シーン（場面）」は丸括弧、一文を超える補足はNote/Infoブロック）
- WS-071（「ビューポート変換」節の本筋の後に「### ビューポート変換とy軸の反転」「### ビューポートの縦横比」で注意点を追補）
- WS-072（[幾何学的変換][同次座標][階層モデリング][投影の手順]等へのリンク委譲）
- WS-073（「[右手系と左手系]の違いがあるからです。x軸を右、y軸を上に向けたとき、z軸の正の向きを手前にとるのが右手系、奥にとるのが左手系でした。」）
- WS-026（記事末「モデリング座標系の中だけで定められていた形状の座標は、こうして`4`つの変換を通り抜け、画面上の位置として決まります。」）
  ※ WS-005：数式・行列を扱う記事だが、末尾に次記事への橋渡し文はなく、4変換を通り抜けたことを圧縮する一文で閉じる（後続への誘導は本文中のInfoブロックに分散している）
