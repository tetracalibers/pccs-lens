分析時点: `6e22b863`
記事タイプ: 未記録

- WS-004（概念の位置づけ直し「Yxy表色系は、XYZ表色系の別名と考えることができます」で終える）
- WS-015（各 `##` の第1文が前節の結論をそのまま言い直して次へ渡す。「色度が `x,y` の2つの値だけで表せるということは、色度は2次元平面上の点で表せることになります」。数式の導出が積み上がる記事では節の第1文に固定的に現れる）
- WS-030（`x+y+z=1` の式→「実はこの関係があるおかげで、色度は `x,y` の2つの値だけで表すことができます」の受け文）
  ※ WS-013：リード自体を持たず frontmatter 直後に `##` が来る
  ※ WS-034：`###` に命題文の見出しが1件（「色度は2つの値だけで表せる」）。常体の短文で置かれ、`##` は名詞句から外れない。用例1件のため積極的な指示にはしない
- WS-042（比率に明るさが混じる→数値例→「そこで割合を考えることにします」）
- WS-052（「このように、明るさの影響を取り除いて、色みだけを表す比率を、:Anki[色度座標]といいます」。見出し語は先に定義、説明中の副次語は後で命名）
- WS-055（丸括弧1句で意味を与える「:Anki[色度]（色み）」「:Anki[彩度]（色みの鮮やかさ）」）
- WS-062（TermCard の図解は予告なし、明度別の色度図を積み重ねた図は「次の図は、明度の異なるxy色度図を順に積み重ねたものです。」の予告つき）
  ※ 図の後ろで図中の要素（スペクトル軌跡・純紫軌跡）を解説する箇所があり、「説明を終えてから図を置く」順序は絶対ではない
- WS-068（Note＝「色度座標は、大文字の `X,Y,Z` で表される三刺激値と区別するため、小文字の `x,y,z` で表されます。」という表記上の混同防止。中身を1点に絞る）
- WS-072（前提知識の節を持たず、色覚の特性・XYZ表色系をその場でリンクにして通過する）
  ※ WS-078：実務的含意を持たず概念の言い換えで終端する対照例
