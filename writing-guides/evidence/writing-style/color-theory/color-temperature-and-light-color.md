分析時点: `0a79bd68`
記事タイプ: 未記録

- WS-004（図解コンポーネント `<SVGWrapper><ColorTemperatureScale /></SVGWrapper>` で閉じる。後続の話題を持たないため橋渡し文ではなく図解）
- WS-018（「照明光の色みを…といい、光源の分光分布によって決まります」。`##`／`###` すべてで例外なし）
- WS-030（本文中の全リストが予告文とセット。「市販のランプの多くは、色温度を使って光色を表しています。」のように件数を伴わない予告文もある）
- WS-032（否定型の転換「光色を表す方法は、分光分布だけではありません。」→色温度の導入。1記事1回で、導入文は肯定の定義文）
  ※ WS-013：リード自体を持たず `<script>` の直後に最初の `##` が来る
- WS-040（1段落1論点・1〜3文。言い足しは改行で同じ段落に連ね、観点が変わるところで段落を切る）
- WS-042（「このような「物体の温度」と「物体が発する光の色」との関係から、色温度という概念が生まれました」）
  ※ 節冒頭の定義文（「照明光の色みを…といい、…」）は前文を受けない
- WS-058（`:::Example` の使用は0回）
  ※ 本筋の説明が完結したあとの体感的な例（ろうそく・ガスバーナー）は `:::Note` に切り出す
- WS-059（「ろうそくなどの赤い炎」）
- WS-069（「単位には:Anki[K]（読みは:Anki[ケルビン]）を使います」）
