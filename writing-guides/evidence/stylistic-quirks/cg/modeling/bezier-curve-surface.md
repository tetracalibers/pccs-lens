分析時点: `9d00ec19`
記事タイプ: 概念解説

- SQ-001（全体が敬体。「まずは直線を描くことで、この考え方を探ってみましょう」）
- SQ-002（「$t$ を動かすことで、点 $C(t)$ が…変化します」動詞終止／「これに $t$ のべき乗と $1 - t$ のべき乗をかけ合わせた形になっていて」）
- SQ-003（本文は敬体、:::Actionは「…注目しよう」「観察しよう」と常体）
- SQ-006（リード2文「[パラメータ形式]は、…扱いやすい表し方でした。しかし、…手がかりがありません。」記事自身への言及なし）
- SQ-007（「この補う操作を:Anki[補間]、直線の場合は特に:Anki[線形補間]（linear interpolation）と呼びます」「形を決めるために置くこれらの点を:Anki[制御点]といいます」）
- SQ-009（「制御点を順に線分で結んだ折れ線を:Anki[制御多角形]といいます」「この規則性を表した関数を:Anki[バーンスタイン基底関数]といい」）
- SQ-012（Note「`lerp`はlinear interpolationの略、`mix`は、まさにある比率…で座標値を混合することを表していますね」）
- SQ-013（「`lerp`や`mix`という名前で呼ばれることも多くあります」「この曲線は:Anki[2次ベジェ曲線]と呼ばれます」）
- SQ-015（「そこで、関数を直接考えるのをやめ、いくつかの点の置き方で曲線の形を操れるようにしたのが:Anki[ベジェ曲線]です」）
- SQ-017（「ベジェ曲線は、この折れ線の角の内部で丸まったような曲線になります」）
- SQ-018（「点 $C(t)$ が線分上で制御点 $P_0, P_1$ のどちらに近づいていくが変化します」（疑問詞節の名詞化））
- SQ-019（「各制御点にどれだけの:Anki[重み]をかけるかを決める関数」「$n$ 個から $i$ 個を選ぶ組み合わせが何通りあるかを表す数」）
- SQ-020（「たとえば、$n = 1$（直線）の場合の式は次のようなものでした」）
- SQ-026（「$C(t)$ は $t$ の`2`次多項式として表されるため」「`1`回ごとに $1 - t$ と $t$ のどちらかがかかるため」／「分母が`1`になるため」）
- SQ-027（「そのため、$B_{i,1}(t)$ の部分は次のようになります」）
- SQ-030（「この規則性は高次になっても続き」「この重みを表すバーンスタイン基底関数のグラフを」「この性質は、ベジェ曲線の振る舞いにおいて非常に重要になります」）
- SQ-032（「そして、この性質は`3`次に限らずどの次数でも成り立ちます。つまり、次の式が成り立つのです」）
- SQ-034（「これは $n = 3$ の場合であり、`4`つの基底関数は次のようになります」）
- SQ-035（「[パラメータ形式]は、…扱いやすい表し方でした」「直線を表すには`2`つの制御点、`2`次ベジェ曲線では`3`つの制御点が必要でした」）
- SQ-036（末尾「…どんな振る舞いに繋がるのかは、[ベジェ曲線の数学的性質]で見ていきます」）
- SQ-037（同上「見ていきます」で後続記事の扱いを表す）
- SQ-038（「次の式で表されます」「次のようになります」）
- SQ-039（「$Q_0(t)$ と $Q_1(t)$ に線形補間の式を代入して展開すると、`3`つの制御点だけで書かれた式になります」）
- SQ-040（「そこで、関数を直接考えるのをやめ、…のが:Anki[ベジェ曲線]です」）
- SQ-042（「$P_0, P_1$ の間を点で埋めていくことは」「点が始点から終点へ動いていきます」）
- SQ-044（「`3`つの制御点だけで書かれた式になります」）
- SQ-047（「:Anki[線形補間]（linear interpolation）」「$n = 1$（直線）」「$n = 2$ の場合（`2`次ベジェ曲線）」）
- SQ-049（「わかります」「たとえば」「といえます」「かけ合わせた」）
  ※ SQ-049：「繋ぐ」を漢字で書く（「`2`つの線分が $P_1$ で繋がっているとし」「$P_0, P_2$ の間を繋ぐ曲線が現れます」「どんな振る舞いに繋がるのか」）。ひらがな書き選好が表外訓の一部には及んでいない
- SQ-050（見出し「### 線形補間（1次ベジェ曲線？）」に全角「？」）
- SQ-055（「`0`から`1`の間」「`4`つの重み」「`2`次多項式」）
- SQ-056（「`lerp`や`mix`という名前で呼ばれることも多くあります」）
- SQ-057（「各制御点にかかる重み $B_{i,n}(t)$ の式には、規則性があることが知られています」）
- SQ-058（「各制御点にどれだけの重みをかけるかを決める関数と捉えることもできます」「$P_0$ と $P_1$ を重みつき平均した式といえます」）
- SQ-059（「$P_1$ には $1 - t$ の重みがかかっているのです」「つまり、次の式が成り立つのです」「高次のベジェ曲線の式も求めることができるのです」）
- SQ-060（「これらの係数を使うと、`3`次ベジェ曲線は次の数式で表されることがわかります」「…重み付き平均になっているといえます」）
- SQ-061（バーンスタイン基底のデモ直後「このグラフから、重みはどの $t$ でも正の値をとり、すべての重みを合計すると`1`になることがわかります」）
- SQ-063（「一方、:Anki[重み付き平均]は、値ごとの重要度を加味した平均です」（直前の「通常の:Anki[平均]は…」と対））
- SQ-064（「$P_0$ には $t$ の重みがかかり、$P_1$ には $1 - t$ の重みがかかっているのです」）
- SQ-068（「直線の場合は特に:Anki[線形補間]…と呼びます」「特に、重みの合計が`1`の場合は、分母が`1`になるため」）
- SQ-071（リード「[パラメータ形式]は、…扱いやすい表し方でした。しかし、そもそもパラメータを動かして曲線を得るには、…数式を知っている必要があります」）
- SQ-072（「まずは直線を描くことで、この考え方を探ってみましょう」「…$Q_1(t)$ と書くことにしましょう」／Action「観察しよう」）
- SQ-077（「`t`を動かして、点`C(t)`が線分をどう内分するかに注目しよう」「`t`を動かすと、各線分の内分点が動き、`C(t)`が動いた跡が曲線を描くことを観察しよう」）
- SQ-076（「したがって」「ゆえに」等なし。まとめ節を置かず次記事への橋渡しで閉じる）
  ※ SQ-043：5記事中この記事だけ「そのまま」が0回。同じ内容を「もう一度同じ $t$ で線形補間した点」「同様の比率で内分した点」のように、同一性を「同じ／同様」で言い表している
