分析時点: `1557e364`
記事タイプ: 概念解説

- SQ-001（全文が敬体で、専門的内容でも「〜済みます」「〜必要があります」と噛み砕く口調）
- SQ-002（「順に測り直していきます」「反転させて左手系へ移します」＝動詞終止／「この一連の処理が:Anki[ビューイングパイプライン]です」＝定義でのみです）
- SQ-003（本文の敬体 対 Action「見てみよう」「見比べてみよう」「確かめよう」）
- SQ-004（Action内の箇条書きが常体「カメラだけが動き、物体は置かれた場所から動かない」「場面全体が丸ごと逆向きに動く」）
- SQ-006（リード1文「物体に与えられた座標が画面上の位置として決まるまでには、さまざまな座標系を経由します。」）
- SQ-007（「この座標系を:Anki[カメラ座標系]といい」「この座標系を:Anki[投影座標系]といいます」）
- SQ-009（「モデリング座標系で定められた形状の座標を、ワールド座標系での座標へ移す変換を:Anki[モデリング変換]といい」）
- SQ-013（「:Anki[ローカル座標系]とも呼ばれます」「:Anki[視点座標系]とも呼ばれます」）
- SQ-015（「その立体が…直方体に収まるように座標を移すのが:Anki[投影変換]です」「…位置へ移す変換が:Anki[ビューポート変換]です」）
- SQ-018（「それぞれの点が画面のどこに写るかは、まだ求められません」）
- SQ-019（「像がどれだけ傾いて写るかは決まりません」）
- SQ-020（「たとえば、次が一例です」「たとえば、同じ位置に重なった面のどちらが手前にあるかを決める…」）
- SQ-026（「どの段階から見ても扱いにくくなります」に続く「そのため」型／「それを直接には表していないからです」「再計算することになってしまうからです」）
- SQ-027（「そのため、ワールド座標系全体を、カメラ座標系に変換してしまいます」「そのため、この行列は投影変換の行列とあらかじめ掛け合わせて」）
- SQ-030（「この座標系を〜といいます」「これにより、視線がz軸の正の向きになり」「その場合、」）
- SQ-032（Note「つまり、カメラ座標系上で行った変換の、:Anki[逆行列]をかければよいわけです」）
- SQ-035（「z軸の正の向きを手前にとるのが:Anki[右手系]、奥にとるのが:Anki[左手系]でした」）
- SQ-036（「詳しくは[階層モデリング]で扱います」「[投影の手順]で詳しく扱います」「[投影の計算法]で組み立てます」）
- SQ-038（「次の`4`つの変換を経て、画像上の位置を決定します」「たとえば、次が一例です」）
- SQ-040（「そこで、目的に合った座標系をいくつも用意し」「そこで、写す範囲を切り取る前に、:Anki[z軸]の向きだけを反転させて」）
- SQ-042（「形状の座標を…順に測り直していきます」「大きさは拡大/縮小で、向きを回転で、置き場所を平行移動で決めていきます」）
- SQ-043（「視線に沿って測った奥行きが、そのままz座標になります」「この`3`つの規定が、そのままカメラ座標系の骨組みになります」「x座標とy座標はそのままなので」）
- SQ-044（「頂点`1`つあたりの計算は`1`回の掛け算で済みます」「座標値の:Anki[大小]を比べるだけで点が内側にあるかどうかを判定できます」）
- SQ-045（「どの段階から見ても扱いにくくなります」「前後を比べる処理では扱いにくくなります」）
- SQ-047（「:Anki[座標系]（座標の測り方）」「:Anki[シーン]（場面）」「:Anki[カメラ]（視点）」）
- SQ-049（「いくつも用意し」「装置に依らない形へそろえた分」「はみ出した部分を取り除く」）
- SQ-050（Action「ビューポートの縦横比が画像に写す範囲の縦横比と食い違うと、像はどのように引き伸ばされる？」）
  ※ SQ-050：全角「？」の問いかけはActionブロック内に限られ、本文の地の文には現れない。埋め込み疑問句は読者操作の指示文でのみ使う条件付きの癖。
- SQ-052（「1. :Anki[モデリング変換]：物体ごとに決めた座標を、場面全体で共通の座標へ移す」）
- SQ-053（「平行移動・拡大/縮小・回転の組み合わせ」「x・y・zのどれをとっても」「光源やカメラなども置かれます」）
- SQ-054（:Anki[負]／:Anki[小さい]／:Anki[逆向き]／:Anki[上]／:Anki[下]と、値や向きの語まで強調に含める）
- SQ-055（「`4`つの変換」「`1`回の掛け算」「`-1`にした拡大/縮小」「`4`行`4`列」「`x`座標が`-1`より小さければ」）
- SQ-059（Note「:Anki[逆行列]をかければよいわけです」）
- SQ-062（「再計算することになってしまうからです」「狙った場所とは違うところに物体が配置されてしまいます」「像が引き伸ばされて写ってしまいます」）
- SQ-063（「正規化デバイス座標系では:Anki[上]を正にとるのに対し、画像を扱うときの座標系では…:Anki[下]を正にとるためです」）
- SQ-064（「z軸の正の向きを手前にとるのが右手系、奥にとるのが左手系でした」「カメラだけが動き…／カメラが原点から動かず…」）
- SQ-065（「画像の上での位置を決めるのに使うのは、x座標とy座標だけです」）
- SQ-067（「描画範囲は必ずしも画面全体とは限りません」「`z`座標は画像の上での位置に関わるわけではありませんが」）
- SQ-068（「最初から:Anki[左手系]でカメラ座標系をとる環境では、この反転は必要ありません」「その場合、」）
- SQ-069（「行列の掛け算の順序には注意が必要です」「ここで注意すべきなのが」「ビューポートの:Anki[縦横比]にも注意が必要です」）
- SQ-072（Action「また変換が進んでいく過程を見てみよう」「`2`つの座標系での挙動を見比べてみよう」）
- SQ-076（「したがって」「以上のように」等が無く、末尾も「…:Anki[画面]上の位置として決まります」の平叙で閉じる）
  ※ SQ-039：z軸反転の行列を掲げる箇所では、式の各操作を追うナレーション句を置かず「z座標の符号を変えるだけの操作です」と事前に言い切って式を置くだけ。数式ナレーションはこの記事タイプ（パイプライン概観）では現れない。
