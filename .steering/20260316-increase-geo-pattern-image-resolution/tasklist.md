# タスクリスト：幾何パターン画像の保存・共有解像度向上

## タスク

- [x] `GeoPatternSection.svelte` の `generatePngBlob` で canvas サイズを 300 → 1200 に変更
- [x] `drawImage` の描画先サイズを `(img, 0, 0, 1200, 1200)` と明示し、SVG がキャンバス全体にスケールされるよう修正（省略すると左上 300×300 にしか描画されない不具合）

## 完了条件

- 保存・共有ボタンを押すと 1200×1200px の PNG が生成される
- 色変更・パターン再生成の操作感に変化がない
