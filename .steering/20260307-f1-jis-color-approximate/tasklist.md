# タスクリスト：F1-4 JIS慣用色名の近似表示

## タスク

- [x] `types.ts` に `JISApproximateResult` 型を追加
- [x] `approximate.ts` に `findClosestJis` 関数を追加
- [x] `/approximate/+page.svelte` にJIS近似結果セクションを追加
- [x] 動作確認（型チェック）
- [x] JIS慣用色カードに出題級の右側へHEXコードとコピーボタンを追加

## 完了条件

- `cd app && npm run check` がエラーなく通る
