# CLAUDE.md (プロジェクトメモリ)

## 概要

開発を進めるうえで遵守すべき標準ルールを定義します。

## プロジェクト構造

### ドキュメントの分類

#### 永続的ドキュメント（`docs/`）

アプリケーション全体の基本設計や方針を定義するドキュメント。

- **architecture.md** - 技術仕様書
  - テクノロジースタック
  - アーキテクチャ方針
  - データの扱い
  - 開発ツールと手法
  - コーディング規約の概要

- **repository-structure.md** - リポジトリ構造定義書
  - フォルダ・ファイル構成
  - ディレクトリの役割
  - ファイル配置ルール

- **development-guidelines.md** - 開発ガイドライン
  - コーディング規約
  - 命名規則
  - フォーマット規約
  - スタイリング規約
  - テスト規約
  - Git規約

#### ドメインドキュメント（`docs/domains/`）

アプリケーション特有の用語や前提とする専門知識を定義するドキュメント。

- **glossary.md** - ユビキタス言語定義
  - 色の理論に関する用語
  - アプリケーション内の用語やUI用語（コード上での命名との対応）

- **color-analysis-rules.md** - 色分析ルール定義
  - 色分析ルール定義書
  - 色距離の計算方法
  - PCCSの色相・トーンの構造や表記
  - 配色技法の定義と判定条件

- **image-based-color-rules.md** - イメージ別配色ルール定義書
  - F3で使う配色の説明やサジェストの詳細ルール

#### 仕様ドキュメント（`spec/`）

新機能や仕様変更について、実装に着手する前に `spec-sparring` スキルで対話的に固めた仕様の書き出し先。機能単位で1ファイルを作成し、実装の拠り所とする。

**`spec/` 配下は「実装前に合意した初期仕様」の記録であり、後から更新する必要はない。** 実装時に決めた細部や、実装後に加えた仕様変更を `spec/` に反映させなくてよい（依頼された場合を除く）。書かれている内容が現行の挙動と食い違っていても、それは書き出した時点の合意を残しているだけで、矛盾として直す対象ではない。

- **spec/[スラッグ].md** - 固めた仕様
  - 目的・背景 / スコープ（やること・やらないこと）
  - ユーザーストーリー・主要ユースケース
  - 機能要件 / 既存機能との関係・整合 / ドメインルール上の考慮
  - データ・状態 / エッジケース・異常系
  - 非機能要件 / 制約 / 受け入れ条件
  - 未確定・保留事項

## 開発プロセス

### 機能追加・修正時の手順

#### 1. 仕様の壁打ち（`spec-sparring`）

実装に着手する前に、`/spec-sparring [機能名]` で仕様を対話的に固める。

- ユーザーへの質問で構想を引き出すことを主軸に、論点（目的・スコープ・ユーザーストーリー・機能要件・既存機能との整合・ドメインルール・データ／状態・エッジケース・非機能要件・制約・受け入れ条件）を体系的に網羅する。
- 各論点で前提を疑い、代替案・トレードオフ・リスクを提示して「壁打ち」する。
- **勝手に決めない。** 決定は都度ユーザーに確認し、曖昧な点は「未確定・保留事項」として明示する。
- 合意が取れたら仕様を `spec/[スラッグ].md` に書き出す。ここまでが本工程で、**実装には飛ばない**。

#### 2. 実装

`spec/[スラッグ].md` に固めた仕様に基づいて実装を進める。

#### 3. ドキュメント更新（必要な場合のみ）

今回の作業により、ドキュメント（`docs/`）の内容と実際の実装やディレクトリ構成に矛盾が生じる場合や、`docs/domains` に追記すべきドメイン用語がある場合は、ドキュメントの更新を行う。

## ドキュメント管理の原則

### 永続的ドキュメント（`docs/`）
- アプリケーションの基本設計を記述
- 頻繁に更新されない
- 大きな設計変更時のみ更新

### 仕様ドキュメント（`spec/`）
- 機能単位で `spec-sparring` の成果物として作成
- 実装の拠り所となる合意済み仕様を記録
- 未確定・保留事項を明示し、決定の経緯を残す
- **書き出した時点の初期仕様であり、実装後に追随させない。** 現行の挙動の正典は実装（`app/**`）とスキル定義（`.claude/skills/**/SKILL.md`）側にあるので、仕様変更のときはそちらを直す
- 仕様変更で内容が古くなっても、**古いこと自体は問題ではない**（決定の経緯として残す）。`spec/` を更新するのは、ユーザーから明示的に依頼されたときだけ

## 注意事項

- 実装前に `spec-sparring` で仕様を対話的に固め、各段階でユーザーの合意を得る
- 仕様は勝手に確定させず、推測で埋めた点・曖昧な点は「未確定・保留事項」として明示する
- 永続的ドキュメントと仕様ドキュメントを混同しない
- **`spec/` 配下は初期仕様なので、仕様変更のたびに更新しない**（現行の挙動は実装とスキル定義が正典。→「ドキュメント管理の原則」）
- コード変更後は「品質チェック」節の基準に従ってリント・型チェックを実施する
- セキュリティを考慮したコーディング（XSS対策、入力バリデーションなど）

## Git規約

コミットメッセージは必ず日本語で記述する。

**コミットメッセージは事前の承認を求めず、組み立てたものをそのままコミットする。かわりに、コミット完了後に件名を報告する。** scratchpad などの一時ファイルには書き出さず、ヒアドキュメントで `git commit -F -` に直接渡して、メッセージがコマンド上で読める形にする。本文まで含めた全文の再掲は不要。

`.svx` ファイル（記事ページ）のコミットメッセージは、次の規約に従う。

- AIが書いた初稿の場合：`記事タイトル [ai-draft]`
- 初稿ではないが、AIによる修正しか含まない場合：`記事タイトル：修正内容 [ai-draft]`
- 人間が加えた修正のコミットを依頼された場合：`記事タイトル：修正内容`

ただし、AIによる修正しか含まない場合であっても、`author-style-writer` スキルの編集モードで指示された修正については、`[ai-draft]` を付与せず、人間が加えた修正のコミットを依頼された場合と同様のコミットメッセージにする。この場合、依頼された編集であることがわかるように記述する：`記事タイトル：依頼された修正内容`

  - 例：`景観色彩の考え方：依頼に基づき導入文を追記`
  - 例：`住宅の色彩設計プロセス：依頼に基づき第2節の言い回しを修正`

依頼に基づく修正をコミットするときは、件名の変更内容だけでなく、**どんな依頼・なぜそう変えたか（指示の意図・背景）**をコミットメッセージ本文に日本語で残す。後から履歴を見て判断理由を辿れるようにするため、件名だけで意図が伝わらない場合は本文に数行添える。

  - 例（本文）：`「〜ことではありません。〜です。」の否定→定義型が他記事と丸かぶりで機械的に見える、という依頼を受けての修正。`

ドキュメントとして扱われる（アプリの実行に関わらない）Markdownファイルのみの変更や、スキルの変更は、`[skip ci]` をつけてコミットする。

`generate-ogp-image` スキルによる OGP 画像の生成結果のみのコミットも、`[skip ci]` をつけてコミットする。生成物（`app/static/ogp/**`・`ogimage/data/**`）・マニフェスト（`app/src/lib/meta/og-manifest.json`）・タスクリスト（`ogimage/OGP-TASKLIST.md`）は `app/src/lib/meta/og-manifest.json` を含むが、いずれもアプリのロジックではなくメタデータ・静的アセットのため CI を回す必要がない。

**ただし、未 push のコミットにアプリの実行やビルドに関わる変更が含まれている場合は、絶対に `[skip ci]` をつけない。** 今回のコミットが Markdown・スキルのみの変更であっても例外ではない。先頭コミットの `[skip ci]` は push 全体の CI をスキップしうるため、`[skip ci]` を付けると未 push のアプリ変更に対して CI が回らなくなる。この場合は必ず `[skip ci]` を外すこと。

**`[skip ci]` を付けないと判断した場合は、件名だけでなくコミットメッセージ本文にも `[skip ci]` という文字列を一切含めない。** CI のスキップ判定はコミットメッセージ全体（本文を含む）に対して行われるため、「規約に従い `[skip ci]` は付けない」のような打ち消しの説明であっても、その記述自体でスキップが有効になってしまう。付けなかった理由を本文に残したい場合は、角括弧つきの表記を使わず「CI スキップ指示は付けない」のように日本語で書く。

## 品質チェック

`app`ディレクトリ内で実装を行ったら、次のコマンドを実行し、エラーや警告が出た場合は修正を行う：

- `npm run check`

ただし、次の基準で実行の要否を判断する。

- ts・js・svelte ファイルなど、型チェックが有効なファイルを変更した場合のみ実行する
- css・svx・md ファイルなどの変更時は実行しない
- svelte ファイルの変更であっても、CSS のみ（`style` 内のみ）の変更であれば実行しない
